# 来源清单与收录门槛

<!-- Copyright 2026 AIC-123 · SPDX-License-Identifier: Apache-2.0 -->

> 本文件回答一个问题：**这里的内容是从哪来的，我怎么核。**
>
> 每一条节点都带 `source.ref`（稳定标识）与 `source.kind`（来源类型）。
> 本文件说明这两项背后的约定，以及**哪些内容我收了、哪些我没收、为什么**。

---

## 一、收录门槛（三条，缺一不可）

1. **能给出稳定标识**——arXiv 编号 / DOI / 官方文档 URL + 章节。
   拿不到稳定标识的内容不收（"某处看到过"不算来源）。
2. **我实际读过被引用的那一段**——不是"知道有这篇论文"。
   这一条排除了大量"我记得好像有研究说过"。
3. **引用强度不超过我读到的范围**——只读了摘要，就不写正文里的数字。

**第 3 条是本清单最重要的纪律。** 下面「读到的范围」一栏就是为它设的：
凡标注"摘要"的，节点里就**不会出现正文级数字**。

---

## 二、一手来源（已实际打开并读取）

### 第一批（22 个节点）

> **⚠️ 2026-09-23 升级**：下表前三行的「我读到的范围」原为**摘要页**，
> 已补读 **HTML 版正文**。读到的范围变了，依赖它们的 8 个节点也同步补了正文级内容。
> 升级记录见本文件 §七。

| 来源 | `kind` | 稳定标识 | 我读到的范围 | 支撑的节点 |
|---|---|---|---|---|
| **DeepSeekMath**（GRPO 原始论文） | `论文` | arXiv:2402.03300；DOI 10.48550/arXiv.2402.03300 | **HTML 版正文**（§4.1.1–§4.1.3、§4.2、§5.2.1–§5.2.3、Table 5） | con-0001、con-0006、stance-0003 |
| **DAPO** | `论文` | arXiv:2503.14476；DOI 10.48550/arXiv.2503.14476 | **摘要页 + HTML 版四项技术小节 + 消融数字** | con-0004、con-0005、judge-0001~0004、stance-0001、arg-0001、case-0001 |
| **Understanding R1-Zero-Like Training**（Dr. GRPO） | `论文` | arXiv:2503.20783；DOI 10.48550/arXiv.2503.20783 | **HTML 版正文**（§2.1–§2.3、§3.1–§3.4、附录 A/B/C/F 含 Table 2/4/5） | judge-0003、stance-0002、arg-0002、case-0002、exc-0001、issue-0001 |
| **DeepSeek-R1** | `论文` | arXiv:2501.12948；期刊版 Nature 645, 633–638 (2025)，DOI 10.1038/s41586-025-09422-z | **HTML 版正文**（§1、§2.1–§2.3、§3、Table 2、**Table 3**、附录 B.3.2） | con-0006、case-0003、judge-0003 |
| **RLHF Book**（Nathan Lambert） | `教材` | rlhfbook.com；arXiv:2504.12501；github.com/natolambert/rlhf-book | **第 6 章「Reinforcement Learning」全文** | con-0002、con-0003、con-0005、judge-0004、judge-0005、exc-0001、issue-0002 |

### 第二批（新增 8 个节点，全部读到正文）

| 来源 | `kind` | 稳定标识 | 我读到的范围 | 支撑的节点 |
|---|---|---|---|---|
| **The Entropy Mechanism of RL for Reasoning LMs** | `论文` | arXiv:2505.22617；DOI 10.48550/arXiv.2505.22617 | **HTML 版正文**（§2.3–§2.6、§3.1–§3.3、§4.1–§4.5 含表 1/表 2） | judge-0007、judge-0008、case-0005 |
| **Stabilizing RL with LLMs**（Qwen） | `论文` | arXiv:2512.01374；DOI 10.48550/arXiv.2512.01374 | **HTML 版正文**（§1、§2.3–§2.4 式 5、§3.1–§3.2 含表 1、§4.1–§4.5） | con-0007、con-0008、case-0004 |
| **Diagnosing Training Inference Mismatch** | `论文` | arXiv:2605.14220；DOI 10.48550/arXiv.2605.14220 | **HTML 版正文**（§2 含表 1、§3.1–§3.2、§4.1–§4.2、§6） | con-0007、judge-0006 |
| **verl 官方文档 · FAQ** | `官方文档` | verl.readthedocs.io/en/latest/faq/faq.html（文档版 2025-09-24） | **「Missmatch between inference and training sequence」整节** | judge-0006、exc-0002 |
| **verl 官方文档 · entropy 配方页** | `官方文档` | verl.readthedocs.io/en/latest/algo/entropy.html（2025-06-27 版） | **整页**（含评估表数字） | judge-0007、judge-0008（数字交叉核对） |

### 关于「没引用的那两条」

**verl 官方文档** 在第一批里只作背景、未逐页核，所以当时没引。**第二批补上了**——
读了 FAQ 页与 entropy 配方页两整页。**这就是"没读就不引"这条纪律的执行方式**：
不是永久排除，是**读到了再引**。

**火山引擎 veMLP「verl GRPO 最佳实践」** 仍**未引用**：读了全文，但它**平台绑定很重**
（AK/SK 配置、vePFS 挂载、队列 ID、提交任务的 yaml 模板）。那些内容对"RL 工程知识"没有普适性，
而剥离平台部分后剩下的又已被 DAPO / RLHF Book 覆盖。**所以不收录，但在此登记，
以免后来的人以为漏了。**

---

## 三、二手来源（**未核验**，不引用）

### 3.1 一批互相转载的博客

本轮搜索命中但**没有打开核验**的：

- 知乎《大模型GRPO训练崩溃整理》
- 知乎《如何改善GRPO的稳定性》
- CSDN《强化学习训练避坑指南：为什么我的GRPO模型奖励不收敛》
- 51CTO 同名转载

**⚠️ 一个必须说在明处的事实**：

> 上面这几条**是同一份内容在多个平台互相转载**。
> **多个博客说同一件事，不等于多个来源证实它。**
> 它们共享同一个原始出处（很可能是同一个人的同一篇），
> 把它们当"多方印证"是统计上的重复计数。

### 3.2 一条被引最多、却过不了门槛的博客

**《When Speed Kills Stability: Demystifying RL Collapse from the Training-Inference Mismatch》**
（ByteDance Seed，Yingru Li 等，2025-09-17）——这个领域里被转述最多的一篇。

**为什么不收**：

1. 它托管在 **Notion**，正文由 JS 渲染。抓取下来**只有 "Notion" 一个词**，
   拿不到任何技术内容 → **违反门槛第 2 条「我实际读过被引用的那一段」**。
2. 搜过，**没有 arXiv 编号也没有 DOI**，拿不到稳定标识 → 违反门槛第 1 条。

→ **不收。** 但它讲的失败模式**已经被一手论文覆盖**（`arXiv:2605.14220`、
`arXiv:2512.01374`），所以**不收也不缺**。

### 3.3 ⚠️ 一处必须更正的结论（第一批写错了）

第一批的 `SOURCES.md` 在这里写过这样一句：

> 「RL 的**诊断类**知识（现象→原因→修法）在公开材料里几乎只存在于二手博客，
> 而一手论文只覆盖"是什么/怎么做"。本产品目前的 22 条节点因此**偏向一手可查的那一半**。」

**这句话是错的。** 错因不在来源，在**搜索词**——当时搜的是
「XX 避坑 / 教程 / 最佳实践」，命中的当然全是互相转载的博客。
换成搜**失败模式本身**，一手来源立刻出现，而且**本身就长成"现象→机理→判据→修法"**。

**修正后的结论**：

> **诊断类内容的一手来源不但存在，而且比"是什么/怎么做"那部分更硬——
> 因为它自带判据数值。**
>
> | 渠道 | 能给出什么 | 门槛 |
> |---|---|---|
> | 把失败模式当研究对象的论文 | 现象 + 机理 + 判据 + 修法 | 有稳定标识，可核 |
> | 框架官方文档的 FAQ / 配方页 | **唯一自带判据数值**（阈值、指标名、触发条件） | 版本相关，要记版本 |
> | 算法论文正文的失败分析段 | 现场描述（**摘要拿不到**，必须读全文） | 要读全文才敢引 |
> | 框架 issue tracker | 真实症状 + 维护者修法 | **只当线索，不当论据** |
>
> **判别器一句话：这条来源写不写得出「判据数值」。**
> 博客说"会崩"；论文与官方 FAQ 说"崩的时候这个指标长什么样、超过多少算坏"。

**这条更正留在这里，不删。** 因为"某个领域只有二手来源"是个**很容易得出、且代价很大**的
假结论——它会让人**放弃一个其实拿得到的来源**。

---

## 四、如果将来要引入二手来源（约定已定，尚未触发）

**不动 Scaffold 的 schema**——`source.kind` 的枚举是 Scaffold 定的，本产品不改它。

二手来源的标注约定（写在本产品自己的 README 里，属于本产品的约定）：

1. `source.kind` 填 `其他`
2. **`notes` 首行必须以「二手来源」四字开头**，并写明平台与标题
3. **永不标 `已确立`**——二手来源支撑不起这个状态

这三条是给后来填的人看的，不是给校验器的（校验器不管这些）。

---

## 五、标注约定（本产品对 Scaffold 字段的用法）

| 字段 | 本产品的填法 | 依据 |
|---|---|---|
| `source.ref` | 稳定标识 + **我读到的范围**（如"摘要页"） | 本文件 §一 第 3 条 |
| `source.kind` | 只填 `论文` / `教材` / `官方文档` / `其他` 四类中的一类 | Scaffold 的闭合枚举（R10 会拦非法值） |
| `evidence_status` | **一律 `未验证`**；有真实分歧的议题填 `有争议` | Scaffold 规矩：`已验证` 只能外部授予，模型填的一律不能是它 |
| `filled_by` | `模型(DeepSeek-V4.1-Flash)` | 不隐藏、不模糊 |
| `notes` | 写"这条的边界在哪、我没读到什么" | 让下一个人知道该往哪补 |

### 一条第二批新增的约定：`官方文档` 类来源必须记版本

论文有 arXiv 编号、版本号、提交日期，天然可定位。**官方文档没有**——它会静默改版。

**约定**：`source.ref` 里对 `官方文档` 类来源，**必须带文档自身的版本日期**
（verl 的文档页脚有 `Last updated: MM/DD/YYYY`）。

例：`verl.readthedocs.io/en/latest/faq/faq.html（文档版 2025-09-24）`

**理由**：本产品**不收配置项**（见 `ADAPT.md` §三），但**收判据数值**——
而判据数值来自官方文档，会随版本变。**不记版本，这条判据就无法被复核**。
配套做法：在 `notes` 里写明"这是单版本的经验线，框架升级后要复核"。

---

## 六、一句话总结这份清单的诚实边界

> **这里的内容是"有出处的整理"，不是"已验证的知识"。**
> 每条都标了来源，但**没有一条被人核验过**——包括我自己：我只核了来源存在、
> 读到了我声称读到的范围，**没有复核论文结论是否可复现**。
> 那是"验证方"的工作，按 Scaffold 的规矩，只有外部授予的 `已验证` 才算数。

**第二批之后可以加一句更准确的**：

> 第二批的 8 条读到了正文，所以它们**带数字**。但**数字可核 ≠ 结论成立**：
> 数字是"论文这么写的"，不是"我复现过"。这两件事之间的距离，
> 正是 `evidence_status` 一栏存在的理由。

---

## 七、2026-09-23 升级：把「只有摘要」的那批补成正文

**做了什么**：把第一批里只读到**摘要页**的三篇论文——DeepSeekMath、Dr. GRPO、DeepSeek-R1——
补读 **HTML 版正文**，然后把依赖它们的 **8 个节点**同步补上正文级内容。

| 节点 | 补了什么 |
|---|---|
| `con-0001` | 优势的精确形式与「同组 token 共享」；KL 项**加在损失而非奖励**；「组内相对」与奖励模型的比较性质同源；过程监督变体 |
| `con-0006` | R1 的**准确性 + 格式**两项奖励；**R1 明确弃用神经奖励模型**及其理由；DeepSeekMath 的 Rule/Model 之分；两条 caveat |
| `stance-0003` | 论文给的**两条**具体理由（同规模 / 只有末 token 有奖励分）；**没有内存节省数字**；**没有严格 PPO 对照实验** |
| `stance-0002` | 偏差是**两个**不是摘要说的一个；与 **RLOO** 的等价性；开源 PPO 实现也有同类偏差；基座侧 6 组数字 |
| `judge-0003` | 长度分布表（**621.3/1038.9 → 4965.4/8206.1**）；**观察本身被混淆**；8.2k step 的外部变量锚点 |
| `arg-0002` | 三类证据（长度分布 / 算力成本 / 准确率表）；**"新 SOTA" 只在 AIME24 单列成立** |
| `case-0002` | 43.3 的出处是 Table 4、模型名 Oat-Zero-7B；**8×A100 约 27 小时**；基座对照 6 组数字 |
| `case-0003` | **Table 3 的 8 个基准数字**（AIME24 77.9、MATH-500 95.9…）；自洽性 86.7；R1-Zero 的两条代价 |
| `exc-0001` | **「代价」的答案**：两个来源对同一项的叫法不同（教材叫代价、论文叫偏差），且**都没量化** |
| `issue-0001` | 新增**第 0 层与第 1 层**：外部参数变量、以及「观察本身被混淆」 |

### ⚠️ 这次升级暴露了一个流程漏洞，必须留痕

补读时发现：**`con-0001` 的 `source.ref` 当时写着「§4.1」，而本文件的表里写着「摘要页」。**

> **也就是说：节点声称的引用范围，超出了当时实际读到的范围。**
> 这正是本文件 §一 第 3 条要拦的事——**它没被拦住**，
> 因为**拦住它需要一个交叉核对，而当时没有做**。

**所以：`source.ref` 里写章节号，本身不构成"读过了"的证据。**
现在这一条由本文件的「我读到的范围」列来兜底，但**兜底靠的是人去看**。
如果将来要自动化，应该加一条检查：**节点 `source.ref` 里出现的章节标记，
必须能在本文件的对应行里找到。**（本轮**未实现**，登记为欠账。）

### 这次升级不改的东西

- **节点数量不变**（30），**类型分布不变**——补的是内容深度，不是覆盖面
- **`evidence_status` 全部仍为 `未验证`**：读到正文不等于复现过
- **`source.kind` 枚举未动**


---

## 八、第五批：一个**没有独立来源**的节点，怎么诚实写 `source`

第五批只加了一个节点（`judge-0009`），它**不引入任何新的外部来源**。
它的 `source` 该怎么写，是本批唯一的新问题。

| 节点 | 性质 | `source.ref` | `source.kind` | 为什么这么写 |
|---|---|---|---|---|
| `judge-0009`（分诊） | **组合承载**：内容全部由它指向的节点推出 | 「组合承载：本节点不引入新的来源，它是既有节点的分诊路由。四条分支的依据分别在 con-0006、judge-0002、judge-0004、con-0001 的 source.ref 里。」 | `其他` | 它**不产生事实，只产生路由**。给它编一个论文出处是**把路由冒充成发现** |

### ⚠️ 同批的 `case-0006` 已删除——**它不是案例，是一道题**

同批还建了 `case-0006`（案例位，两项来源留空）。**第六批确认它是举例，已删除**，
理由与经过见 `ADAPT.md` §十三。这里只记与来源有关的两件事：

**一、它当时撞出的上游 R9 盲区，结论仍然成立**（不依赖那个节点存不存在）：
R9 的判据是「**写了 `kind` 没写 `ref` → 警告**」，而 `访谈口述` 这一类
**根本没有 `ref` 可写**（没有 DOI、没有章节、没有链接）。
→ **`访谈口述` 在 R9 下没有一个"诚实且不报警"的状态。**

当时的处置是两项都留空（`ref` 空 = R2 的合法状态，不触发 R9）。
**但要说清：那是绕过，不是解决**——它让来源性质只能写进 `notes`，而 `notes` 不参与任何检查。

**二、它现在是一条"没有实例的纪律"。** 本文件 §一 第 1 条要求「稳定标识符」，
而访谈口述给不出稳定标识符——所以**这类来源本产品现在一条都没有**。
纪律留着，等真的收进访谈来源时再用。

### 一条纪律，本批再次确认

**别给「亲历案例」编 `source.ref`。**
"缺出处"（R2 的合法状态）与"出处只能是人"形式上一样，**性质不同**：
前者是不知道从哪来，后者是**只能从使用者来**。
给它编一个 DOI，就是把"某人说的"伪装成"文献说的"。

> ⚠️ **2026-09-23 更正：这条只管「亲历案例」，不管「论文实验案例」。**
> 旧版这句话写成了"别给案例编 `source.ref`"，于是和库里的实际情况打架——
> `case-0001`~`case-0005` **全部带 `arXiv:` 出处**，看起来像违规。
> 实际不是：那 5 个是**论文实验案例**（读论文读出来的实验记录），
> 它们的出处就是论文，**必须有 `source.ref`**。
> **「案例」这个词在两个意思上被混用了**，见 `README.md` 的「谁填的」那一行。
> 判据一句话：**出处能不能落到文献上？** 能 → 论文实验案例，写出处；
> 只能落到某个人身上（访谈、口述、你的实验）→ 亲历案例，**留空**，不编。

### 本批没有新增任何外部来源

`judge-0009` 的内容全部由既有节点推出。所以 §二 的两张来源表**一行未变**。

---

## 九、第六批：**只删不加**，来源表一行未动

第六批的动作是 **32 → 31**（删掉 `case-0006`，没加任何节点）。

- **没有新增任何外部来源**——所以 §二 的两张来源表仍然**一行未变**
- **没有修改任何节点的 `source`**
- **`judge-0009` 的 `source` 一个字没改**——它本来就写对了，
  第六批改的是**路径模板有没有去读它的正文**（见 `ADAPT.md` §十三）

### 补的 8 节「怎么确认」用的是**已有来源**，不是新来源

第六批给 `judge-0001`～`judge-0008` 各补了一节「怎么确认」（共 28 步）。
**这 28 步没有一步来自新读的材料**——全部取自各节点**自己 `source.ref` 里已经记着**的东西
（DAPO §3.1/§3.2、Dr. GRPO 附录 F、R1 §2.1、verl FAQ、2505.22617 §2.3–§2.6 与 §4.2）。

**但要说清一件事**：其中**有一部分是我把论文的机制读出来的，不是论文给的日志字段**。
例如：

- `judge-0001` 第 3 步（看被裁掉的 token 落在哪一侧）——DAPO 给的是机制解释
- `judge-0002` 第 1 步（按组算奖励标准差）——DAPO 给的是现象与后果链

→ **这两处都在节点正文里明确标了来源强度**，没有混进"论文说的"里。
**这是 §一 第 3 条（引用强度不得超过读到的范围）在"方法"这一层上的应用。**

### 一件与来源无关、但值得记在这里的事

第六批把对方举的那个例子做成了一条**带内容断言的用例**，
留在 `tools/find_path.py` 的 `SELF_TEST` 里。它**不是来源**，也**不是案例**。

为什么记在 `SOURCES.md` 里：因为最容易犯的错就是**把题当成事实收进来**。
上一批我就是这么错的（把对方的举例建成了 `case-0006`）。

> **一道题不需要来源，一条案例必须有来源。**
> 混了这两样，就会出现"看起来有案例、其实只是有人随口举了个例子"的结构。

⚠️ **而那道题也不需要新家**：使用者指出「如果遇到这种情况怎么办」**就是症状口吻**，
走正常检索就行。我一度给它单开了 `SCENARIO_TEST` 表与 `--scenario` 命令——
那是把已有入口包装成新类别，已并回。见 `ADAPT.md` §十三。


---

## 十、第七批：**零新来源**，四个新节点引的都是既有来源

**本批没有打开任何一个新来源**，`§二` 的两张来源表**一行未动**。
新增的 4 个判断点（`judge-0010`~`0013`）全部是**对已有来源的重新组装**：

| 新节点 | `source.ref` 里引的 | 那段来源**什么时候读的** |
|---|---|---|
| `judge-0010` 判分规则的覆盖 | R1 §2.2；DeepSeekMath §5.2.3 | 第三批补读时读的正文（见 §七） |
| `judge-0011` 格式分 | R1 §2.2、Table 3 | 同上 |
| `judge-0012` 过程分 | DeepSeekMath §4.1.3、§5.2.3；R1 §2.2 | 同上 |
| `judge-0013` 长度惩罚 | DAPO §3.4、§4；Dr. GRPO 附录 F Table 5 | 第一批 / 第三批读的 |

### ⚠️ 但有两处的「引用强度」要交代清楚

按 §一 第 3 条（**引用强度不得超过读到的范围**），下面两句必须标明性质：

1. **`judge-0010` 的"等价写法清单"** —— 来源**只给了"用规则比对答案"这个判分方式**，
   **没有给**一份"常见漏判形态"的清单。那一节是我**按机制推出来的工程做法**，
   节点 `notes` 里已标"这是我读出来的，不是来源给的清单"。
2. **`judge-0012` 的"把过程分拆成两种来源"** —— 两条来源**各只说了一半**
   （DeepSeekMath §4.1.3 说机制上支持分步，R1 §2.2 说弃用神经过程奖励），
   **没有任何一条把这两半摆在一起**。把它们合起来是**我做的**，节点 `notes` 里已标。

→ 这两处都不是"编来源"，是**把来源的边界写出来**。区别在于：
**前者是"来源说了 A"，后者是"我由 A 推出 B"——B 必须标明它是 B。**

---

## 十一、第七批续：新增**一个**外部来源（`C2 选型` 补的那半个）

`C2 选型` 分两半，其中「必须先联网找来源」那半，**只找到并只收了这一条**：

| 来源 | `kind` | 稳定标识 | 我读到的范围 | 支撑的节点 |
|---|---|---|---|---|
| **SFT vs RL: A Study of Post-Training Methods for LLMs** | `论文` | arXiv:2603.13985；DOI 10.48550/arXiv.2603.13985（2026-03-14, v1） | **HTML 版正文的 §6 Common Practice and Takeaways 整节**（+ 摘要页确认作者/日期） | `judge-0014` |

### ⚠️ 这条来源的引用强度，三句话交代

1. **它是综述，不是控制变量实验。** 它给的是"领域里的常见实践"，**不是"我们做了对照得出"**。
   所以 `judge-0014` 全篇的措辞是"来源说常见做法是…"，**不写成"实验证明"**。
2. **我读到的是 §6 那一节**，不是全文。所以 `source.ref` 里写的是 `§6`，
   **不出现 §4/§5 的章节号**——按 §一 第 3 条，引用强度不超过读到的范围。
3. **§6 里那四条结论是并列陈述的**；`judge-0014` 把它们排成"先问什么"的次序，
   **那一步排序是我做的**，节点 `notes` 里已标。

### ❌ 同一半里，这两类**没有来源，所以没建节点**

- **「该用现成框架还是自己写训练循环」** —— 全库 grep 0 命中，本轮也没联网搜到合格来源
  （搜索命中的多是框架自己的宣传页或二手对比博客，过不了 §一 的门槛）。
- **「我该选 verl 还是 OpenRLHF」「手上有 8 张卡该跑多大」** —— 同上。
  而且**这类问题本身是"参数型/配置型"内容**，按 `ADAPT.md` 的既有裁决，它的正确位置是官方文档，
  **本结构不收**（收了只会得到一个会过期的副本）。

→ 所以 `C2` 是**部分完成**：**「要不要上 RL」建了节点；「框架/规模选型」仍然空白，且这不是漏了，是有意的边界。**
