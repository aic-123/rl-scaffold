# rl-scaffold · 已定案与工作规则

<!-- Copyright 2026 AIC-123 · SPDX-License-Identifier: Apache-2.0 -->

> **这份文件的唯一目的：让"已经定过的事"有一个单一落点，不再被反复重开。**
>
> 加它的原因是一个实测出来的现象：本产品已被使用者定案的三条
> （处境化归一 / 不另造类别 / 铺选型与事前奖励设计），
> 在 `GAPS.md`、`PLAN.md` 里**仍被列为"待拍板"**——
> 于是每换一个上下文，读到的都是"还没定"，就从零再问一遍。
> 这不是记性差，是**没有一处写着"已定"**。见 `AUDIT.md` §四 根因 3。

---

## 一、开工前必读顺序（按这个顺序读，别跳）

| # | 文件 | 读它是为了知道什么 |
|---|---|---|
| 1 | **本文件** | 哪些事**已经定了**、不许重开；哪些规则必须守 |
| 2 | `PLAN.md` | 产品定位、批次进度、**明确不做什么** |
| 3 | `AUDIT.md` | 当前已知缺陷、根因、下一批的执行顺序 |
| 4 | `ADAPT.md` | 从上游 Scaffold 到本领域的**历次技术决议及其理由** |
| 5 | `GAPS.md` | 缺口清单（**是待办，不是验收项**） |
| 6 | `SOURCES.md` | 来源清单 + 收录门槛 + 引用范围纪律 |

**⚠️ 要动检索/视图/正文之前，先读 1 和 3。**
**要重新讨论一个方向之前，先在本文件 §二 里 grep 一遍那个方向。**

---

## 二、已定案清单

> **状态说明**：`生效` = 照做，不许重开；`已被取代` = 留着让人知道它**曾经**是决议（**不删**）；
> `待拍板` = 真的还没定，可以讨论。
> 推翻一条 `生效` 的决议，走 §四 的流程。

---

### D-01 · 上游 `ks-demo` 一个字不改 `生效`

**背景**：本产品建在 Scaffold（`ks-demo` v0.0.3, commit `f140642`）的规格上，
复用它的 `validator/` 与 `schema/`。
**决议**：`validator/` 与 `schema/` **逐字节拷贝**，来源 commit 记在 `VENDORED.md`；
本仓库**不修改**上游任何文件。要改规则就去上游改。
**后果**：本仓库的检查能力上限 = 上游校验器的能力。
**所以"承诺兑现"类检查只能加在 `tools/` 这边**（`find_path.py --check` / `render_view.py --check`），
不要指望 `validate.py` 代劳——它按分工只管"结构自不自洽"。

---

### D-02 · 处境包含"事前决策"，不限于故障；指南式问法归一成处境后匹配 `生效`

**背景**：曾把问法分成「处境」和「指南」两类，并打算给指南类另做一套。**这条分法是错的。**
**决议**：
- **处境 = 已有条件 + 目标 + 约束 + 不确定性 + 故障症状。** "我打算这么做，行不行"**就是处境**。
- **指南式问法（"学习率设多少""该用什么评测集"）也是处境的一种**——
  归一的做法是**提取用户已经交代的条件与约束**，然后用**原本那句话**去匹配。
- ⚠️ **归一不得添加用户没有交代的事实**：不能替他补上"你在防发散""你显存不够""你的任务是数学"。
  信息不足就**列出待确认项**，不替他填。
- **不新增"指南"节点类型**，也不为它新开一层。
**判据**：`PLAN.md` §一 禁的是**给数**（超参推荐表），**不是收下这个问题**。
把"不给答案"读成"不收入口"是过度解读。

---

### D-03 · 不为「外部用例 / 出题口吻」新开类别，也不新开表 `生效`

**背景**：有人举了个例子问「这种情况你怎么办」，一度被建成 `case-0006`（案例位）。
使用者一句话点破：**「出题其实就是症状啊，如果遇到这种情况怎么办，不用新加」。**
**决议**：
- 「如果遇到 X 怎么办」**就是症状口吻**，走**正常检索**，**从来不需要新家**。
- 它是**用例**（进 `SELF_TEST`），不是**节点**（不进 `nodes/`）。
  **一道题不需要来源，一条案例必须有来源。**
- ⚠️ **不许为外部用例另开一张表、另开一个命令、另加一个字段。**
  外部用例的"外来身份"写在 `说明` 里就够了。
**一句话记法**：**新造一个类别之前，先问一句——这是不是已经有的那个东西换了个说法？**
**代价**：空壳案例比没有案例更坏，因为它骗的是读结构的人。

---

### D-04 · 判断点作为主入口——但改判只在"分数同量级"时发生 `生效`

**背景**：`ADAPT.md` §一 定了"判断点作为主入口"。旧实现把它做成
`判断点分数 ≥ 阈值 × 0.8` 就无条件改判，而这个 0.8 **从没写进规格**。
**实测后果**：概念节点自身得分 **1.90** 的查询被一个 **0.27** 分的判断点顶掉；
把库里 81 条处境原句回灌，**13 条没回到自己的节点**。
**决议**：改判条件改为（已写进 `MATCH_SPEC["入口优先判断点"]`）：
① 最高分**不是**判断点；② 存在判断点：过阈值、`cue_dice > 0`、
且**分数 ≥ 最高分 × 0.5**。改判必须**打印出来**（含采集工具里）。
**后果**：回灌里"没回到自己节点"从 **13 → 3**，其余 3 条是
**节点间 cue 近乎重复**导致的有意改判（`case-0005`/`con-0004`/`exc-0002`），需要人再看一眼。
**⚠️ 0.5 是拍的数，但它写在规格里、带着两组实测**——
拍数不可怕，"拍完了不写下来"才可怕。

---

### D-05 · 适用范围必须真的输出给使用者 `生效`

**背景**：路径对象里有 `scope`，但渲染时**一个字都不印**。
**实测后果**：查询「我的奖励来自奖励模型，reward 崩了」→ `judge-0009`（**1.40**），
而该节点 `scope` 第一句就是「**不适用于奖励来自奖励模型或人工标注的场景**」。
→ 系统给出了**明确不适用于用户**的答案，而用户看不到那句排除条件。
**决议**：文本输出与 HTML 路径块**都必须印 `scope`**，两道守卫分别落在
`find_path.py --check`（第 7 条）与 `render_view.py --check`（第 8 条）。
**代价**：改完之后"scope 是死字段"这件事不成立了——
`MATCH_SPEC` 与 `README` 里"scope 参与匹配"的说法**要么真做，要么收窄措辞**（见 §三 待办）。

---

### D-06 · 承诺要有守卫：加一类承诺，同一次改动里加一条会红的负例 `生效`

**背景**：`MATCH_SPEC["路径模板"]` 承诺 ① 里有**三件**（变量 / 怎么测 / 判据），
而检查只断言 ① 非空。**实测**：把 `judge-0005` 的整个 `## 怎么确认` 删掉（只改内存），
`--check` **依然退出码 0**、依然打印"全部命中"。
**决议**：任何"正文里承诺的结构"都要有可执行检查。
**验收问法不是"这个检查跑绿了吗"，而是「我把它删掉/改坏，它会红吗？」**
**后果**：`--check` 现在钉七件事，其中第 6、7 条都是这一条决议的产物，
**并且各自实测过负例**（删节 → 点名 `judge-0005`；拿掉 scope 输出 → 点名 `case-0001`）。

---

### D-07 · 权威度永不进排序；来源与证据状态在解析阶段就丢掉 `生效`

**继承自上游 Scaffold 的硬规矩。**
`_match_record()` 只放行 `id / type / title / aliases / cues / scope / relations`；
`source`、`evidence_status`、`filled_by` **在内存里根本不存在**。
**所以"来源不进匹配"不是承诺，是打分函数拿不到它们**——`--check` 会实测这一点。
**排序键只有「文件路径，然后 cue 文本」。**

---

### D-08 · 「案例」的两个意思必须分开写 `生效`

**背景**：`README` 曾写「本结构当前一个案例节点都没有」，而库里**有 5 个 `type: 案例`**。
**不是节点错，是词被当成两个意思用了。**
**决议**：凡是说到"案例"，必须点明是哪一个：
- **亲历案例**（Scaffold 第⑤层本义：情境特征 / 考虑过的选项 / 理由 / 结果 / 反事实）
  —— **一个都没有**，模型一个字都不能填，`source.ref` **留空**（不许编）。
- **论文实验案例**（`case-0001`~`case-0005`，从论文读出来的实验记录）
  —— **有 5 个，全部由模型填**，`source.ref` **必须有**（就是那篇论文）。
**判据一句话**：**出处能不能落到文献上？** 能 → 论文实验案例，写出处；
只能落到某个人身上 → 亲历案例，留空。

---

### D-09 · 素材方向：先把"已定"的两块铺出来 `生效`

**背景**：缺口清单实测——四个入口里 **排查者满、选型者基本空**；
`B 事前 · 判分规则设计` **8 句缺 7**。
**决议**：按"**来源是否已在库**"分两批：
- **C1 `B 事前 · 判分规则设计`** —— 来源全在库里（`con-0006` / DAPO / R1 / `judge-0004` / `judge-0009`），
  **纯组装，不用找新来源**。优先做。
- **C2 `C 选型`** —— 必须拆两半：`issue-0002` / `con-0002` 那半**改 cue 就能通**；
  「要不要上 RL」「框架选型」「8 张卡跑多大」那半 **全库 grep 0 命中，必须先联网找来源**。
  **不先找来源就动笔，就是编。**

**落地情况（2026-09-23 第七批）**：
- ✅ **C1 已做**：新增 `judge-0010`（判分规则的覆盖）/ `judge-0011`（格式分）/
  `judge-0012`（过程分）/ `judge-0013`（长度惩罚）**4 个判断点，零新来源**。
- ✅ **C2 部分完成**：
  - 「改 cue 就能通」那半**已通**——`issue-0002` 补了「GRPO 和 PPO 我该选哪个」等 cue，
    从落错 `exc-0001`（0.50）变为正确落位（1.90）。
  - 「必须先联网找来源」那半**只建成一个**：`judge-0014`（要不要上 RL），
    来源 `arXiv:2603.13985` §6，已按综述性质标注引用强度。
  - **「框架选型」「8 张卡跑多大」仍然空白——那不是漏了，是有意的边界**：
    没搜到过得了门槛的来源，而且这类是**参数型/配置型**内容，正确位置是官方文档。
    见 `SOURCES.md` §十一。

---

### D-10 · 缺口数不是覆盖率；**同义改写命中率**要单独当指标量 `生效`

**背景**：第七批补完 4 个节点后，`probe_gaps.py` 的"缺口"从 23 掉到 16。
**那个掉幅大半是假的**——新节点的 `cues` 里**写了语料原句**，
所以命中是**构造出来的**：不是"接住了"，是**把测试集喂成了训练集**。

**实测（这才是证据）**：

| 时点 | 留出集（与 cues 不同的说法） | 命中 |
|---|---|---|
| 补完节点、未加 cue | 9 句 | **4/9** |
| 按自然说法补完 cue 后，**换一套全新句子** | 9 句 | **1/9** |
| 两套并起来 | **17 句** | **9/17** |

**决议**：
1. **两个数分开报，永不合并**：
   - **缺口数**（`probe_gaps.py`）—— 回答"哪些处境还没人接"，
     **但它对"往 cues 里抄句子"是敏感的，会虚高**；
   - **同义改写命中率**（`probe_gaps.py --held`）—— 回答"换个说法还找不找得到"，
     **它才是入口可用性的尺子**。
2. **留出集的句子不许进 `cues`。** 进去了就不再是留出集，要补就换新句子重测。
3. **不许用"往 cues 里多抄几句"来压这个数**——那是把测试集喂成训练集。
4. **`A 排查者` 那组"对照组全中"要降级读**：那 4 句和对应 cues **几乎逐字相同**，
   它证明的是"我把像的话写进了 cues"，不是"匹配器没问题"。
   （第六批当时的结论下早了，此更正。）

**后果 / 由此暴露的真问题**：
**检索的瓶颈不在内容量，在匹配算法。** 中文走**字符二元组重合**（Dice），
需要**字面共享**；真人换一套词说同一件事，就落不下去。
→ 这是一条**新的、独立的**待办（见 §五），**不是"再多加点节点"能解决的**。

**判据**：凡声称"覆盖变好了"，必须同时给出**留出集**上的数，且留出集与 `cues` 无交集。

---

### D-11 · 阈值与门槛都由**实测扫描**定，并且**门槛只卡判断点** `生效`

**背景**：同义改写命中率只有 9/17，初看像"匹配算法不认同义改写"。
**实测发现不是**：那 8 句"没匹配上"的，**正确节点已经全是最高分**——只是没过阈值。
→ 所以先调阈值，**不动匹配算法**。

**实测扫描**（左=留出命中 / 右=越界负例最高分）：

```
0.30 → 9/17（0.14）｜0.22 → 13/17（0.14）｜0.18 → 15/17（0.14）
0.16 → 15/17（0.14）｜0.14 → 15/17（负例过线）｜0.12 → 17/17（负例过线）
```

**决议**：
1. **阈值 `TAU` = 0.18。** 取它是**能到 15/17 的最大阈值**，因此安全边距也最大（0.04）。
2. **新增门槛：判断点必须靠"一句话"进 —— `cue` 或 `标题` 命中都算，`别名/标识符` 不算。**
   说人话：**我接住的必须是你的一句话，不能是你的一个词。**
   - **只卡判断点**：一刀切会把面试者入口打死（「组内归一化是怎么算的」靠名字进 `con-0001`，`cue_dice=0.00` 而**合法**）——实测过。
   - **`标题` 也算"一句话"**：只认 `cue` 的话，「熵掉得太快，还剩多少可以烧」会被判成没匹配上（它靠标题命中，分数 0.64）——那也是**把用户的一句话当成了不是**，实测过。
3. **三处必须走同一个函数** `pick()`（CLI / 自检 / 采集工具）——否则"算不算匹配上"会有三种答案。

**后果**：留出 15/17；「靠一个词硬落下来」的可疑命中 4 → 1；
其中一条是真落错被抓出（「我要不要上 RL」曾落到 `judge-0013` 长度惩罚，靠的只是个 `rl`）。
**代价**：可能把"本来能兜住、只是 cue 写得不好"的查询判成没匹配上——
**可接受：宁可说「没匹配上」并列出最近的几条，也不要给一个靠名字猜的答案。**

**⚠️ 边距只有 0.04。** 所以「离得更近的越界句」必须进 `SELF_TEST` 当负例钉住
（已把「我的显卡坏了怎么办」0.14 与「公司不给我批算力」0.11 补进去，负例总数 2 → 4）。

### D-12 · 长口述：**先拆句再匹配**，并补一项长度归一 `生效`

**背景**：使用者拿**他自己那段 150 多字的原话**试系统，系统回「没匹配上」。
**实测原因是长度稀释**：中文走字符二元组，Dice 的分母是两边长度之和，
而 `cues` 都只有 20 字上下——
那句长口述与**最像的那条 cue** 的 `Dice` 只有 **0.081**，而**重合系数是 0.400**（差 5 倍）。

**决议（两条，都实测过）**：

1. **打分里加 `0.6 × 重合系数`**（`|A∩B| / min(|A|,|B|)`），**与 Dice 取 max，不是相加**。
   - 实测：长口述 0.15 → **0.24**（接得住）；越界负例最高 0.150（**边距从 0.04 变宽到 0.09**）。
   - ⚠️ **不能单独用重合系数**：它不含长度惩罚，长句里嵌一句短话就接近满分。所以只能**加权后取 max**。
   - ⚠️ 门槛 `accepts()` 必须**用同一份证据**（`cue_dice` / `0.6×重合` / `title_dice` 取 max），
     否则会出现「分数 0.46、却被门槛挡住」——**分数和门槛打架**。这踩过一次。
2. **使用者指出：「可以先对这句话拆分再找系统」。** → 落成 `--split`：
   按**标点和空格**切成同粒度的句子，逐句匹配，再按节点合并。
   - ⚠️ **空格也算分隔符**：实测踩过——当事人原话常用**空格**断句
     （「我的意思是 首先你得有个基线 比如说…」），只按标点切**整段才切成 2 段**，等于没拆。
   - 拆出来的引导语（「我的意思是」）**不专门过滤**，让它自然被阈值挡掉——少一条要维护的规则。
   - 合并后**优先展开判断点**（与 D-04 一致），并对非判断点的命中标注「可能只是词面接近」。
   - 实测：15 段里 **3 段**落到 `judge-0009`（0.80 / 0.62 / 0.59）。

**守卫**：`SPLIT_TEST` 钉住那条真实口述——**至少 3 段落到 `judge-0009`**。
负例验过：把期望入口改成 `judge-0001` → `--check` **退出码 1** 并点名。
⚠️ 断言的是「**至少几段**」，不是「全都对」——一段口述里必然混着引导语和跑题的话，**那些本来就该落不下去**。

---

---

## 三、工作规则（R-1 … R-8）

| # | 规则 | 检查方式 |
|---|---|---|
| **R-1** | **先读已定案，再开口。** 开工前按 §一 的顺序读。**已定案的不重开** | 任何"要不要 X"的提问，先 grep 本文件 §二 有没有 X 的结论 |
| **R-2** | **不许有隐藏拍数。** 任何阈值/比例，要么进 `MATCH_SPEC` 这类规格常量**并写明理由**，要么删掉 | `grep -n "0\.[0-9]" tools/*.py` 的每一处，都能在规格或注释里找到解释 |
| **R-3** | **说"缺内容"之前，先排四件**：① 匹配器/路由 → ② 正文提取（判据、怎么测）→ ③ 适用范围是否被输出 → ④ 真缺节点。**没跑完 ①②③ 不许跳到 ④** | 报"缺节点"时，报告里必须出现前三步的实测输出 |
| **R-4** | **缺口判定不许只靠阈值。** 过阈值 ≠ 相关；不过阈值 ≠ 缺知识 | 构造语料**只能证伪**，报告里不许写成"覆盖率" |
| **R-5** | **加承诺的同时加检查。** 见 D-06 | 新增一类承诺 → 同一次改动里加一条会 FAIL 的负例 |
| **R-6** | **文档里的每个数字，改完数据立刻 grep 一遍** | 每条计数声称都要有 grep 命令 + 输出 |
| **R-7** | **不确定的推断必须标出来。** 标 `【推论】` 或写"（无外部来源）" | 不许混进"节点说的" |
| **R-8** | **缺口数与同义改写命中率分开报，永不合并。** 声称"覆盖变好了"必须给出**留出集**上的数，且留出集与 `cues` **无交集**（见 D-10） | `probe_gaps.py` 与 `probe_gaps.py --held` 两个数都要出现在报告里 |

---

## 四、怎么推翻一条已定案

**不许静默改。** 要走完四步，缺一不可：

1. **指名**：推翻哪一条（写编号，如 D-04）。
2. **给新证据**：实测输出 / 上游规格原文 / 可核的外部来源。**"我觉得"不算。**
3. **写代价**：改了之后**哪些已经通过的东西会变红**、哪些文档要跟着改。
4. **留痕**：把旧条目标 `已被取代`（**不删**），写明取代它的新条目的编号。
   按上游 Nygard 的 ADR 做法：**"它曾经是决议"这件事本身有价值。**

---

## 五、真正待拍板（可以讨论）

| # | 事项 | 卡在哪 |
|---|---|---|
| 1 | **`MATCH_SPEC` 里"scope 参与匹配"要不要真做** | 已查明**不该做**：scope 含「不适用于…」，参与匹配后**恰在查询落进排除范围时把该节点抬得更高**，方向是反的。已改为**输出端印出** + 文档收窄措辞。**剩下的是"自动警示"要不要做**——试过两种判据（Dice 0.194 vs 0.146；LCS 8/4 vs 4），**分离度都不够，已放弃**。除非你要求硬上 |
| 2 | **`C2` 剩下的「框架/规模选型」** | 没搜到过门槛的来源，且属**参数型内容**（按既有裁决不收）。**要不要破例收？** 默认不收 |
| 3 | **亲历案例谁来填** | 这是模型填不了的边界（D-08）。**只有使用者能给。** |
| 4 | **仓库命名** | 目录名 `rl-scaffold` 是占位名 |
| 5 | **发布** | 中英双语 README 已有中文侧；英文侧与发布节奏等你定 |

> ✅ **`git init` 已完成**（2026-09-23，首次提交 `0410c20`，身份 `AIC-123 <noreply>`，
> `.gitattributes` 钉死 LF）。原本列在这里的第 2 条待拍板项，已消掉。

---

## 附 · 本文件自己怎么被检查

**它也是文档，也会腐烂。** 所以：

- 本文件里凡是提到**数字**（13 条 / 3 条 / 0.5 / 8 句缺 7），都必须能当场复现；
- 复现方式见 `AUDIT.md` 附录；
- **改完数据记得回来 grep 一遍本文件**（R-6）。
