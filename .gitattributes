# 行尾策略：钉死 LF。
#
# 为什么必须先有这个文件、再归一化文件（顺序不能反）：
# 只归一化而不钉策略，编辑器下一次一写就回到 CRLF，缺陷会**复发**。
#
# 本文件不是装饰：本产品把「纯文本、逐行可 diff」当核心性质，
# 而混合行尾会让 diff 变脏——那是肉眼看不见的。
* text=auto eol=lf

# 生成物：可重新生成，但仍然入库（本产品要求视图可 diff）
index/*.md   text eol=lf
index/*.html text eol=lf
