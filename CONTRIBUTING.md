# 参与这个项目

> **先读三段话，再动手。** 这三段省掉的是最常发生的三类无效劳动。

**一、这里的内容全部由模型填、全部标 `未验证`。** 这不是待修的缺陷，是**设计**——
按 Scaffold 的规矩，`已验证` **只能由外部授予**。所以"帮我把状态改成已验证"这类请求会被拒绝，
而且不是态度问题，是这结构能成立的前提。

**二、分歧只列不裁。** 遇到有真分歧的地方，本结构建**议题 + 立场**节点，两边都挂，**不替读者下结论**。
"你给个结论吧"不是这个项目要做的事。

**三、最缺的不是内容，是"一句话"。** 如果你只想帮一件事，就帮这件——
把你**真实遇到过的一句话**丢给检索器，然后把输出贴进 issue。见下面「最有价值的贡献」。

---

## 开发环境

```bash
git clone https://github.com/aic-123/rl-scaffold.git
cd rl-scaffold
python -m pip install -r requirements.txt   # 只有一条依赖：PyYAML
```

**不需要 `pyproject.toml`，不需要装成包。** 这是刻意的——本项目要能 `clone` 下来就跑。

## 动手前跑这四条

```bash
python validator/validate.py nodes        # 结构自洽，期望 ERROR 0 / 退出码 0
python validator/validate.py --self-test  # 校验器自检，期望退出码 0
python tools/find_path.py --check         # 检索自检（25 条用例 + 4 负例 + 长口述）
python tools/render_view.py --check       # 两个视图同序
```

**四条都绿再动手。** 有一条不绿就说明你的基线是脏的——那样你会分不清
后面冒出来的红是你造成的还是本来就有的。

改了 `nodes/` 之后**必须重建视图**（视图是生成物但**入库**，本产品要求视图可 diff）：

```bash
python tools/build_index.py && python tools/render_view.py && git status --porcelain
# 最后这条必须**没有输出**
```

---

## 最有价值的贡献：一句话

本项目的自我检查里有一句原话：

> **用自己造的用例测自己的系统，测的是自洽，不是覆盖。覆盖要靠外部场景来撞。**

所以**你的一句话比十次代码审查有用**。做法：用[「检索落错了」模板](https://github.com/aic-123/rl-scaffold/issues/new?template=retrieval.yml)，
逐字粘贴你的原话 + 完整输出。

⚠️ 提交时请如实区分：这句话是你**真实遇到**的，还是**你构造**的？
前者能进留出集（测"改写鲁棒性"），后者只能进语料（找内容缺口）。
**两者都有用，但混为一谈就是所有这类项目翻车的同一个姿势。**

---

## 加节点

先读 [`DECISIONS.md`](DECISIONS.md)（已定案的别重开）和
`scaffold-node-filling` 那套纪律（如果它在你的技能库里）。硬要求：

| 要求 | 为什么 |
|---|---|
| **必须有 `source.ref`** | 空值合法，但**不许编**。"出处只能是人"的（访谈/口述）留空，不伪造 DOI |
| **引用强度不得超过实读范围** | 读的是摘要就别写"§4.1"。这条纪律**被违反过一次**，记录在 [`SOURCES.md`](SOURCES.md) §七 |
| **`filled_by` 如实写** | 模型填的写模型。**`已验证` 不许自声明** |
| **判断点要有 `变量：` 行，且 ≤4 个** | 多了就说明它其实是两个判断点 |
| **判断点要有 `## 怎么确认`** | ① 承诺「变量 → 怎么测 → 判据」三件；**只给量名不给测法等于没给** |
| **`scope` 必须写"不适用于"** | 只写"适用于什么"等于没写边界 |

**三条最容易犯的**：

1. **别往 `cues` 里抄测试句子。** 写"当事人会怎么问"，不是"怎么让测试通过"。
2. **别新增节点类型。** 那要改 Scaffold 的 schema，而本项目**不改上游**。
   想加一个类别之前先问：这是不是已经有的那个东西换了个说法？
3. **案例不能由举例生成。** 「如果遇到这种情况怎么办」**就是症状口吻**，走正常检索，
   不需要新家。留一个空壳案例位，等于给"看起来有案例"留了个假证据。

## 加规则 / 改检索

**先说清一件事**：检索规格全在 `tools/find_path.py` 的 `MATCH_SPEC` 里，
**阈值和门槛必须能自证**——带着实测扫描表（横轴阈值，纵轴真阳性命中率 + 越界负例最高分）。

- ⚠️ **不许有隐藏拍数。** 任何阈值/比例要么进 `MATCH_SPEC` 并写明理由，要么删掉
- ⚠️ **判定"算不算匹配上"必须只有一处实现**（`pick()`）。CLI / 自检 / 采集工具共用它
- ⚠️ **门槛必须用和分数同一份证据**，否则会出现「分数够高、却被拒绝」

## 不许做的事

| 不做 | 理由 |
|---|---|
| 改 `validator/` 或 `schema/` | 它们是上游 Scaffold 的**逐字节拷贝**。要改就去上游改 |
| 给节点排序 / 加权 / 打分 | **排序即推荐。** 把权威度写进排序，就是替读者决定该信谁 |
| 把 `source` / `evidence_status` / `filled_by` 放进匹配 | 现在它们在**解析阶段就被丢掉**——打分函数拿不到它们。这是一条**由代码结构保证**的红线 |
| 用模型做匹配 | 模型提名入口 = 不可复现、不可核对的"自称" |
| 收录私有配置 / 推荐值 | 详见 [`ADAPT.md`](ADAPT.md) §三 |

---

## 提交

- 一个 PR 一件事
- **PR 里贴实际运行输出**，不要写"我跑过了"——本项目文化是「分数不可核对，依据可以」，同一条适用于 PR
- 提交信息写清楚**"这个缺陷是什么形状、怎么被发现的"**，而不只是"修复了 X"

## 关于分歧

这个项目**公开邀请分歧**（议题/立场节点就是干这个的），但有三条底线：

1. **对有依据的反对意见不设门槛** —— 这是项目存在的理由
2. **人身攻击、骚扰、歧视** —— 不接受。见 [`CODE_OF_CONDUCT.md`](CODE_OF_CONDUCT.md)
3. **要推翻一条已定案的决议，走 `DECISIONS.md` §四 的四步**：指名 / 新证据 / 代价 / 留痕。
   **旧条目不删，只标"已被取代"**——"它曾经是决议"这件事本身有价值
