# 结构适配决议

<!-- Copyright 2026 AIC-123 · SPDX-License-Identifier: Apache-2.0 -->

> 阶段 3 的产出：**拿 RL 的真实内容形态去撞 Scaffold 的九类节点**，
> 记录装不下的，逐个决定怎么办。
>
> 结论先说：**九类节点够用，但有一处真实缺口——配置项。本产品选择不收录它。**

---

## 一、撞的结果

| RL 里的内容形态 | 装到哪 | 结果 |
|---|---|---|
| 机制陈述（GRPO 怎么算优势） | `概念` | ✅ 直接装下 |
| 归因主张（长度增长怪谁） | `立场` + `议题` | ✅ 装下，且**正是分歧层存在的理由** |
| 实验证据（消融链） | `论据` | ✅ 装下 |
| 具体一次实验（谁在什么配置下得到什么） | `案例` | ✅ 装下 |
| **诊断**（现象 → 看哪几个变量 → 对策） | `判断点` + `例外` + `立场` | ⚠️ **装下，但需要一个节点协作，不是一个节点装完** |
| 配置项（`ppo_mini_batch_size` 该设多少） | —— | ❌ **没有承载它的类型**（见 §三） |
| 时效说明（这条基于哪个版本） | `scope` 字段 | ✅ 装下（见 §四） |

### 「诊断」这条值得展开

RL 里最有价值的内容形态是 **现象 → 原因 → 修法**（如"熵快速下降 → 上界裁剪限制了探索 token → Clip-Higher"）。
九类里**没有一类能单独装下这三段**。实际是怎么装的：

- **现象 + 看哪几个变量** → `判断点`（如 `judge-0001`）
- **归因的争论** → `议题` + 两个 `立场`（如 `issue-0001` + `stance-0001`/`stance-0002`）
- **对策的具体效果** → `论据` / `案例`（如 `arg-0001`）

**这是特性，不是缺陷。** 因为"原因"在 RL 里**本来就有争议**——
DAPO 和 Dr. GRPO 对同一个现象给了不同归因。硬把"现象→原因→修法"塞进一个节点，
等于**强迫写节点的人当场裁决**，而那正是本结构要避免的事。

**决议**：**不新增类型。** 用组合承载，并在索引页里把 `判断点` 作为主入口。

---

## 二、九类里哪几类用上了、哪几类没用

（用量为两批合计 30 个节点）

| 类型 | 用量 | 说明 |
|---|---|---|
| `概念` | 8 | 主承载。第二批新增「训练-推理偏差」与「一阶近似」——它们是诊断类内容的理论底座 |
| `判断点` | 8 | **主入口**，每条的 `cues` 都是处境句 |
| `条件` | **0** | ⚠️ 见下 |
| `例外` | 2 | 形态是「例外覆盖」。第二条是**版本相关的临时例外**（见 §七） |
| `议题` | 2 | 真实分歧 |
| `立场` | 3 | 挂在议题上 |
| `论据` | 2 | 都指向立场 |
| `断言` | 0 | 至今没用到——没有需要单独成节点的事实性断言 |
| `案例` | 5 | 都指向它作为实例的那条论据 / 立场 / **概念**（第二批两条指向概念） |

### ⚠️ 发现：`条件` 与 `scope` 字段功能重叠

本批 **0 个 `条件` 节点**。原因：RL 里"这条在什么前提下成立"几乎全是**适用范围**问题，
而 Scaffold 的 `scope` 字段定义就是「适用范围，**必须同时写清不适用**」——
它已经把 `条件` 想装的东西装走了。

**决议**：**本产品不主动建 `条件` 节点。**
只有当某个前置条件是**独立的知识**（自己值得被索引、被别的节点引用）时才建。
把适用范围写进 `scope`，不拆成节点。

**这是一条产品级约定，不是结构改动**——Scaffold 的 `条件` 类型仍然可用。

---

## 三、❌ 真实缺口：配置项没有承载它的类型

RL 工程里最常见的一类内容是**配置项**：`ppo_mini_batch_size`、`micro_batch_size`、
组大小 `G`、`L_max`、`clip_ratio`……它们既不是概念，也不是判断点。

**三个选项与我的选择：**

| 选项 | 评价 |
|---|---|
| 硬塞进 `概念` | ❌ 会稀释概念层——概念层装的是**机制**，配置项是**旋钮**，两者混在一起索引就废了 |
| 新建一个类型 | ❌ **要改 Scaffold 的 schema**。本产品的定位是"引用上游，不修改上游" |
| **不收录** | ✅ **选这个** |

**不收录的三个理由：**

1. **时效性极短**。RLHF Book 自己的 changelog 显示一年多就大改版并重组章节；
   框架的配置项在版本间会变。收进来会快速腐烂，而腐烂的节点比没有节点更坏——
   它会让人以为这里是权威。
2. **它的正确位置是官方文档**。`verl` 的文档本来就是为此存在的。
   本结构复制它，只会得到一个会过期的副本。
3. **它不是"知识"，是"参数"**。参数没有"在什么条件下成立"这一说，
   只有"在我的硬件和版本下跑不跑得动"——那是实测，不是知识。

**已写进 `README.md` 的「已知的未做」**，不是漏写。

---

## 四、时效怎么承载（没有新增字段）

RL 变化快，所以每条工程性断言都要能回答"基于哪个版本/什么配置"。
**没有新增字段**，用了 Scaffold 已有的 `scope` + `notes`：

| 信息 | 放哪 | 例 |
|---|---|---|
| 这条在什么条件下成立 | `scope` | case-0001 的 scope 写明「Qwen2.5-32B **base**、AIME 2024、avg@32」 |
| 这条基于哪个时点 | `notes` | case-0002 的 notes 写明「那是作者在 2025-03 的自述，不替它背书'至今仍是 SOTA'」 |
| 我没读到什么 | `notes` | case-0003 的 notes 写明「摘要层面没有数字，所以本节点不写数字」 |

**约定**：`notes` 里写"边界与缺口"，不写"结论"。这样 `--todo` 能靠 `notes` 找欠账。

---

## 五、一个真实的结构压力点（记录，本轮不处理）

**`evidence_status` 是单值的，但 RL 里同一件事在不同层面状态不同。**

具体例子：**KL 惩罚**。
- **理论层面**：仍是标准组件，教材用一整节讲它
- **工程层面**：随 RLHF 转向推理与可验证奖励（RLVR），**许多推理向改编版完全关闭它**

单值的 `evidence_status` 装不下"理论上还在、工程上已经不用"这种状态。
本轮的处理是**用 `scope` + 正文说清**（见 `judge-0005`），**不改字段**。

**为什么不改**：改 `evidence_status` 就要改 Scaffold 的 schema，
而这是一条会影响所有领域的结构性改动，不该由 RL 这一个领域的需求驱动。
**如果第二个领域也撞上同一个问题，那才是改上游的信号。** 这条留在这里，作为那个信号的一部分。

---

## 六、一条本产品自己定的约定（Scaffold 明确留给使用者的）

Scaffold 的 SPEC 规定：`relations` 的方向有约定，但**「案例指向谁、朝哪个方向指，
本规格不作约定」**——留给使用者自定。

**本产品定的方向**：

> **案例 → 它作为实例的那条论据 / 立场 / 概念。**

理由：案例是**证据**，证据指向它支撑的结论。反向关系写在正文里，
不写进 `relations`（写了就构成环，被 R3 判错——这是设计使然，见 Scaffold `SPEC.md`）。

---

## 七、第二批新增的一条边界裁决：`例外` 可以收版本绑定的内容，配置项不行

**看起来像破例的地方**：§三 说不收配置项，但 `exc-0002` 收了一条
「非 Hopper 卡（A100/L20/B200）+ vLLM issue 22103 + 长文本」的例外——
它**明确绑着具体硬件型号和框架版本**，官方文档还说**修复已在路上**。

**为什么不是破例**——判据只有一条：

> **问它「是不是一个可以照抄的数值」。**
> 是 → 配置项，**不收**（它回答"旋钮拧到几"）。
> 不是，它是个**成立条件** → 可以收（它回答"这条判据在什么条件下才成立"）。

`exc-0002` 约束的是 `judge-0006` 那条判据**在什么硬件上成立**，
它不是一个可以照抄的参数值。**它属于判据的一部分。**

**配套纪律（已执行）**：这类内容必须在 `scope` 里写清版本边界，
并在 `notes` 里写明**它会过期**。原文照录：

> 如果有一天你发现这条已经过期，那不是它写错了，是环境变了。

**顺带定的一条约定**：`官方文档` 类来源的 `source.ref` **必须带文档版本日期**
（论文天然有 arXiv 编号与版本，官方文档会静默改版）。理由与写法见 `SOURCES.md` §五末。

---

## 八、第二批把「诊断类内容从哪来」这个问题解决了（结论已修正）

第一批在 `SOURCES.md` 里得出过一个结论：「RL 的诊断类知识几乎只存在于二手博客」。
**那个结论是错的**，错因是**搜索词**——搜「避坑 / 教程」当然只命中互相转载的博客。

换成搜**失败模式本身**，一手来源立刻出现，而且**本身就长成"现象→机理→判据→修法"**。
四条渠道、判别器与更正全文见 [`SOURCES.md`](SOURCES.md) §三。

**对结构的影响**：**没有影响。** 第二批 8 个节点用的还是原来的九类，
承载方式与 §一 的「组合承载」完全一致——
只是**内容从"一手可查的那一半"扩到了诊断那一半**，
其中 `概念`（`con-0007`、`con-0008`）提供理论底座，`判断点` 提供入口，
`案例` 提供实测，`例外` 提供边界。

→ **这是「不新增类型」这个决议的第一次压力检验，它扛住了。**

---

## 九、第三批（补正文）：结构零改动，但暴露了一个流程漏洞

**做了什么**：把第一批里只读到**摘要**的三篇论文补读 HTML 正文，
依赖它们的 8 个节点同步加厚。**节点数仍是 30，类型分布一字未变。**

### 一条结构性观察：**「读得更深」和「扩覆盖面」是两件事**

- 第二批是**扩覆盖面**：加了 8 个节点，从"是什么/怎么做"扩到"诊断"
- 第三批是**加深度**：节点数不动，只把已有的 8 个节点从摘要级升到正文级

→ **本结构对这两件事的反应完全不同**：
扩覆盖面会改变类型分布（`判断点` 从 5 变 8），加深度**什么分布都不变**。
这说明九类节点承载的是**知识形态**，不是**知识深度**——深度是节点正文与 `source.ref` 的事。

### ⚠️ 暴露的流程漏洞（比上面那条重要）

补读时发现：**某个节点的 `source.ref` 写着章节号（「§4.1」），
而来源清单里那一行写的是「摘要页」。**

> **即：节点声称的引用范围，超过了当时实际读到的范围。**
> 这正是 `SOURCES.md` §一 第 3 条要拦的事，**而它没被拦住**。

**为什么没拦住**：因为拦住它需要**把节点里的章节号与来源清单的「读到的范围」做交叉核对**，
而当时没有任何机制在做这件事。校验器只管结构（字段、类型、关系、枚举），**不管来源范围**——
按 Scaffold 的分工，这本来就不该它管。

**所以得到的结论是**：

> **`source.ref` 里写章节号，本身不构成"读过了"的证据。**
> 它是一种**声称**，需要有另一个地方来核。

**已登记的欠账**（`README.md`「下一批该做什么」第 2 条、`SOURCES.md` §七）：
把这条交叉核对自动化——**节点 `source.ref` 里出现的章节标记，
必须能在来源清单的对应行里找到。本轮未实现。**

→ **这条比任何一个节点内容都重要**：它说明本产品的**核心纪律目前是靠人遵守的，不是靠机器守的**。
`evidence_status` 那一栏守得住"不能自称已验证"，
但**守不住"不能自称读过"**。

---

## 十、第 ⑥ 层（视图层）落地：规格从"注释"变成"可执行的不变量"

**上游的状态**：Scaffold `SPEC.md` 把 ⑥ 视图层定义为
「**查询/排序规格（可见、可改、可版本化）**」，并注明第一阶段 **待定**。
上游仓库里**没有任何 SortSpec 文件**——只有这一行定义。

**本产品此前的状态**：排序规则只存在于两个地方——
生成物 `by-situation.md` 顶部的一段引用块（但那个文件写着"不要手改"），
以及生成器脚本里。**「可见、可改、可版本化」三条只满足了一条半。**

**现在的落地**：

| 要求 | 落点 |
|---|---|
| **可见** | `index/view.html` 顶部把规格原样渲染出来 |
| **可改** | 规格是 `tools/render_view.py` 里的 `SORT_SPEC` 常量 |
| **可版本化** | 它在 git 里，可 diff |

**核心规矩照旧**：排序键只有「文件路径，然后 cue 文本」。
来源 / 机构 / 证据状态 / 填充者**一律不进排序**，也不加权、不打分。
**理由一句话：排序即推荐。**

### 一条新的不变量，以及它为什么必须可执行

两个视图（`.md` 文本视图、`.html` 渲染视图）是**同一份规格的两个形态**，
因此必须给出**逐条一致**的顺序。

**为什么不能只写在注释里**：两个生成器一旦各写各的排序，
会**静默地**给出不同顺序——而"顺序"恰好是这个结构里最敏感的东西（它等于推荐）。
**一个只有注释守护的纪律不是纪律，是愿望。**

→ 所以加了 `python tools/render_view.py --check`：比对两个视图的 75 条处境是否逐条同序、
链接是否全部可解析，**不一致就退出码非 0**。
**并实测了负例**（故意改掉 HTML 里一条的 id，检查确实失败）。

**这正好补上 §九 那个漏洞的一半**：§九 说"来源范围"这条纪律只有人守。
**视图顺序这条，现在机器也守了。**

---

## 十一、把第 ⑥ 层的「查询」那一半补上：按需求检索 → 可行路径

### 起因

上一轮把视图层做成了**平面**：75 条处境平铺，顺序由文件路径决定。
用起来的反馈是「**非常困难**」——因为**你得先知道自己在找什么，才找得到**。

### 一个被漏掉的词

回头看 Scaffold `SPEC.md` 对第 ⑥ 层的原文定义：

> 「**查询**/排序规格（可见、可改、可版本化）」

**「查询」两个字一直在那儿。** 上一轮只做了"排序"，把"查询"当成了"过滤框"。
**过滤不是查询**——过滤要求你先知道关键词，查询要求你描述处境。

### 落地：六步路径

| 步 | 回答什么 | 从哪来 |
|---|---|---|
| ① | 把哪几个量翻出来 | 判断点的 `变量：` 行 + 正文里带数值的判据 |
| ② | 底层机制 | `概念`；直连优先，没有直连就走议题/例外转一手**并标注间接** |
| ③ | 什么时候别信这套 | `例外`（出边 + 入边都算） |
| ④ | 这里是不是有两派 | `议题` → `立场` → `论据` → `案例`（图上 ≤2 跳） |
| ⑤ | 谁真跑过 | `案例`，按挂载强度分三级：直接 / 经论据 / 经概念·弱 |
| ⑥ | 下一步会撞到什么 | 共享概念的其它判断点，标注经由谁 |

**空步必须说"空"，而且要说是「没找到」不是「不存在」**——
本结构的 `relations` 只收有向边，正文里写的反向关系不进图。

### ⚠️ 第一版模板是错的，而且错得很典型

第一版的 ④ 只走**一跳**（`judge → 议题`）。于是 `judge-0001` 的 ④ 是空的。
但 `judge-0001 → exc-0001 → issue-0001` 是**图上真实存在的两步链**——
它恰好是整个结构里最完整的一条路径（一直能走到两个案例）。

**教训**：`relations` 的方向约定是"**论据→立场、立场→议题、例外→被覆盖节点**"，
所以从判断点出发，**议题往往不在第一跳上**。走一跳等于把这条链剪断。

改成**传递闭包**（概念/例外/议题 允许 1–2 跳，标注跳数）后，`judge-0001` 的 ④ 才出来。

### 一个反直觉的结果：**30 个节点的 `relations` 一条都没改**

模板改对之后，`judge-0003` 的 ② 和 ⑥、`judge-0002` 的 ⑤ 也都出来了。
→ 那三个空位**全是模板漏的，不是数据漏的**。

**这件事值得记下来**：结构看起来"缺边"，第一反应往往是去加边。
但这次一验，**图是够用的，是走法不对**。
→ 加边会改变结构；改走法不会。**先怀疑走法。**

### 机制：确定性，明确不用模型

这是本轮唯一一个会碰红线的决定，所以记下理由。

匹配的难点是**你说的话和 `cues` 里的话不一定对得上**
（「模型说话变得很重复」vs「回答越来越像一个模子刻出来的」）。
把模型放进检索路径看起来很自然，但它会**同时撞上本产品已经立住的两条规矩**：

1. `evidence_status` 那一栏守的是「**不能自称已验证**」。
   模型提名入口**本质上是同一种东西**——一个不可复现、不可核对的**自称**。
2. 视图层刚立的规矩是「来源 / 机构 / 置信度**一律不进排序**」。
   模型给候选打分，**就是让一个不透明的东西进了排序**。

**决议：只用确定性算法。匹配不上就如实说匹配不上，不猜。**

### 两条由**代码结构**保证的红线（不是由注释保证的）

**一、来源与证据状态在解析阶段就被丢掉。**
`_match_record()` 只放行 `id / type / title / aliases / cues / scope / relations`。
→ 打分函数**拿不到** `source`、`evidence_status`、`filled_by`。
这不是"我们承诺不看"，是**它们在内存里不存在**。`--check` 里有一条实测这一点。

**二、模糊匹配只有一处实现。**
`view.html` 里**不重复实现打分**——两个实现必然漂移，而漂移的正是顺序（§十 已经吃过一次亏）。
浏览器里只有：字面过滤 + 点开一条**已经算好**的路径。

### 实现中撞出来的三个错，都是同一类

| 错 | 现象 | 根因 | 修法 |
|---|---|---|---|
| **子串当 token** | `judge-0008` 光靠一个 `kl` 就拿到 0.57，而它最像的 cue 重合度是 **0.000** | `"kl" in "klcov"` 为真——「KL-Cov」这个别名把无关节点抬过了阈值 | 改成 **token 集合**匹配；长标识符拆成子词（`actor/grad_norm` → `actor`/`grad`/`gradnorm`） |
| **加法 vs 乘法** | 一个烂大街的词把零重合的节点抬过阈值 | 标识符是**平摊加分**，与文本重合无关 | 改成**乘**：标识符**放大已有的证据**，本来不像，命中一个词还是不像 |
| **入口选错** | 问「β 设多少、能不能不开 KL」，返回的是**概念** `con-0003` 而不是**判断点** `judge-0005` | 概念的 cue「我在纠结 KL 系数设多少」字面上更近 | **入口优先判断点**，并把**改判这件事打印出来**。依据是 §一 已记在案的「判断点作为主入口」 |

**三个错是同一类**：**让一个不构成证据的东西进了排序**。
第一个是"一个常见词"，第二个是"加法"，第三个是"字面最近"。
→ 和 §十 的「排序即推荐」是同一个病。

### 新的不变量（都可执行，且都实测过负例）

`tools/find_path.py --check` 钉住七件事：

1. 匹配记录里没有来源/证据状态字段（且**实测** `_match_record` 真的丢了它们）
2. 每条路径引用的节点都存在（无悬挂 id）
3. 每个判断点都能给出 ①，且 ②/④ 不同时为空
4. **18 条固定用例全部命中，含 2 条负例 + 3 组内容断言**（"今天天气怎么样"必须说没匹配上）
   ——旧版这里写着"12 条"，是第四批的旧数，**已更正**（2026-09-23）
5. ⭐ **每个判断点都读得出 ≥1 步「怎么确认」**（2026-09-23 新增）
   ——旧版只断言 ① 非空，而 ① 非空靠的是 `变量：` 行；实测把 `judge-0005` 的整个
   `## 怎么确认` 删掉，`--check` 依然退出码 0。见 `AUDIT.md` §二.1
6. ⭐ **每个节点有 `scope` 就一定要印进文本输出**（2026-09-23 新增）——
   旧版渲染里一个字都不印，于是会给出**明确不适用**的答案而用户看不见。见 `AUDIT.md` §二.3
7. ⭐ **每个判断点的「怎么确认」每一步都是整行**（旧版已有，未变）

`tools/render_view.py --check` 扩了四条：

8. 「看路径」链接必须与判断点**严格对应**——是判断点的 cue 必须有、不是的必须没有
9. 链接的 `href` 与 `data-open` **两个属性都要核对**，且都要指向真实存在的路径块
10. 标签配平
11. ⭐ **每个路径块里必须印出该节点的适用范围**（2026-09-23 新增）——见 `AUDIT.md` §二.3

**第 9 条是被负例逼出来的**：第一版只查 `href`，
于是把 `data-open` 改坏时检查**通过了**——而 JS 用的是 `data-open`，
用户点下去会展开**别人的**路径。

**第 10 条也是被数出来的**：上一轮生成器**多输出一个 `</div>`**，
页面看起来**完全正常**（浏览器静默容错），只有数标签才发现。
→ **浏览器容错不是正确**。它会一直藏着，直到某次改动让它变成真的错位。

> **「一个只有注释守护的纪律不是纪律，是愿望」——这次是同一句话的第二次应验。**
> 第一次在 §十（视图顺序），这次在 §十一（链接一致性 + 标签配平）。
> 区别是这次连"检查本身"都差点没守住：**只查了一半的属性，等于没查。**

### 与 §九 那个漏洞的关系

§九 说：`source.ref` 里写章节号**本身不构成"读过了"的证据**。
§十一 的做法是同一个思路的推广：

> **任何"声称"都要有一个地方来核。核不了的东西，不进内存。**

`source` 被丢掉是这条；匹配分必须给出**可核对的依据**（命中哪条 cue、哪些标识符、
各分项多少）也是这条——**分数不可核对，依据可以**。

---

## 十二、第五批：补上产品自己声明的那个主入口

### 起因：有人拿外部场景来撞，一撞就穿

上一轮交付"按需求检索"时，我用的是**通用测试用例**（"熵在掉"、"grad_norm 在涨"），
全过了。但拿**一个外部场景**（对方举出的考验用例：「如果遇到这种情况，你的系统怎么办」）
去撞，撞出来的是这个：

```
reward 曲线跑着跑着崩了
  0.48 con-0006   cue=0.000   title=0.000   alias=0.385   ids=['reward']
```

**`cue` 重合 0.000，`title` 重合 0.000。**
这句话和结构里**任何一条处境描述、任何一个标题都没有字面重合**——
它是靠"句子里出现了一个叫 `reward` 的词"才勉强落下来的。

### ⚠️ 而这是产品自己声明的入口

`PLAN.md` §一 第一句：

> 一个 RL 工程师的真实入口不是「我想学 GRPO」，而是**「reward 不涨了」**。

**而 75 条处境里，一条都不是它。**

八个判断点覆盖了熵、grad_norm、长度、KL、全对组、截断、token 干预——
**偏偏漏了最上位的那一个症状。**

→ **这是一次典型的"自测通过了、但没被测到"**：
`SELF_TEST` 里的用例都是我照着**已有节点**写的，
所以它只能证明"已有的入口都能被找到"，**证明不了"入口齐了"**。

> **教训：用自己造的用例测自己的系统，测的是自洽，不是覆盖。**
> 覆盖要靠**外部场景**来撞。

### 决议：新增一个**分诊**判断点（`judge-0009`）

**它不是"多一个节点"，它补的是一层。** 区别在这里：

- `judge-0004` 问的是「**截断样本的奖励怎么给**」——
  那是**已经知道问题出在截断**之后才问得出来的问题
- 但真实的第一句话是「**reward 崩了**」，这时候你还不知道原因

所以 `judge-0009` 只做一件事：把「reward 崩了」拆成**四个可分别去查的方向**，
并给出**怎么确认**的方法（把 rollout 原文和它拿到的分并排逐条比，统计假阴性率，
**再看这些假阴性样本有什么共同点**——如果集中在某个长度段，
那就不是"奖励函数写错了"，而是"奖励函数和长度上限打架了"，两种病修法完全不同）。

**四条分支全部写进 `relations`**（`con-0006` / `judge-0004` / `judge-0002` /
`judge-0001` / `judge-0006`）。理由：**正文说的和图上连的必须一致**，
否则检索会给出正文里有的分支、图上却走不到。

### 一个**不引入任何新来源**的节点

`judge-0009` 是本结构里第一个 `source.kind: 其他` 的节点——
它的内容**全部由它指向的节点推出**，没有独立来源。

**为什么这不算破例**：ADAPT §一 早就定了「诊断类内容用**组合承载**」。
但"组合承载"此前只用在了**单个症状**上（现象→判断点，归因→议题+立场）。
**「症状 → 分诊」这一层是新的**：它不引入任何事实，只引入**路由**。

→ 所以它的 `source.ref` 老实写着「组合承载：本节点不引入新的来源」。

### ✅ 已结案：那不是案例，是一道题

同批我建了 `case-0006`，一个**案例位**（五段只有标题，正文一字未填），
并在 `notes` 里标了风险：使用者描述场景时用的是**举例口吻**
（「**比如说**我现在基于 verl 去跑 qwen3.5 的 GRPO」「跑了 **109 步或者 200 步左右**」），
**如果只是举例，这个节点应当删除——案例不能由举例生成。**

**使用者的答复：**

> 「不，这个不是案例，而是对方举出来的一个随意的例子，是考验如果在这样的情况下应该怎么办」

→ **确认是举例。`case-0006` 已删除。** 见 §十三。

### ⚠️ 顺带撞出的上游规则盲区：R9 对「访谈口述」有盲区

**先说清楚：这条结论不依赖 `case-0006`。** 那个节点只是**撞上**它的实例，
节点删了，盲区还在——因为它来自规则本身的形状：

上游 R9 的判据是「**写了 `kind` 没写 `ref` → 警告**」，
而 `访谈口述` 这一类来源**根本没有 `ref` 可写**（没有 DOI、没有章节、没有链接）。
→ **`访谈口述` 这个 kind 在 R9 下没有一个"诚实且不报警"的状态。**

当时的处置是把 `kind` 与 `ref` **都留空**（`ref` 空 = R2 的合法状态，不触发 R9）。
但要说清：**这是绕过，不是解决**——它让节点的来源性质只能写进 `notes`，
而 `notes` 不参与任何检查。

**不改上游规则**：与 §五 同一条纪律——改 R9 要动上游，
而那是会影响**所有**领域的规则改动，不该由 RL 这一个领域驱动。

→ **记下来，作为"第二个领域也撞上"那个信号的一部分。** 等真的收进访谈来源时再谈。

### 实测效果（同一批说法，补入口前后对比）

| 使用者的说法 | 补前 | 补后 |
|---|---|---|
| reward 曲线跑着跑着崩了 | 0.41（还要**改判**） | **0.93** → `judge-0009` |
| 答案被截断了所以正则没匹配到，reward 判了 0 | 0.31（改判） | **1.52** → `judge-0004` |
| 改完奖励规则 reward 曲线就正常上涨了 | 0.33（改判） | **0.60** → `judge-0009` |
| verl 跑 Qwen 的 GRPO，跑到 200 步 reward 崩了 | 0.49（改判） | 0.50 → `judge-0009` |
| 50% 的 rollout 推理是对的，但 reward 给了 0 分 | 0.34（改判） | 0.41 → `judge-0004` |

**"改判"是指最高分落在非判断点上、被入口规则改判回来**——
补入口前六条里四条要改判，补后基本不需要了。

### 顺带修掉的一个模板缺口

`judge-0009` 一建出来就暴露了路径模板的第五个缺口：
**⑥ 原来只找"共享概念"的邻居，而分诊支路是直连的、不共享概念**——
于是分诊节点的**全部分支都会隐身**。

→ ⑥ 加了「分诊支路」这一遍（入口直连的其它判断点），并把表头改成
**「该去看哪一条（分诊支路）」**（只在真有分诊支路时才这么叫）。

**这是同一条教训的第三次**（前两次：走一跳 ≠ 传递闭包；只查 href ≠ 查了链接）：
**新增一种节点形态，就会暴露模板里一个没被想到的走法。**

### 一条纪律的自我修正（节点已删，纪律留下）

`case-0006` 的第一版 `cues` 我写成了**症状口吻**
（"我跑 verl 的 GRPO，reward 跑到一两百步就崩了"），
结果它和 `judge-0009` **抢同一批查询**——正是 §十一 刚写进 skill 的
「别把同一句话复制到多个节点」。

**修法**：案例的 `cues` 要写成**找例子口吻**，不是**症状口吻**——
对齐 `case-0001` 的既有写法（"我的 GRPO 规模上不去，**想看看别人在类似处境下做了什么**"）。

> **症状口吻指向判断点；找例子口吻指向案例。** 两者的说话人不是同一个人。

**⚠️ 但节点已经删了。那这条纪律还算数吗？**

**算数，而且它反而更硬了**——因为 §十三 里对方那道题**同时**是"症状"和"考验"，
正是最容易写混的那种。

⚠️ **但要纠正一处：我一度以为需要第三种口吻。不需要。**

> 使用者的原话：「**出题其实就是症状啊，如果遇到这种情况怎么办，不用新加**」

对。**「如果遇到这种情况怎么办」就是症状口吻的一种说法**——
说话人仍然是在描述一个处境，只不过用了假设语气。
它走的就是正常检索（`--problem`），**不是一种新的入口**。

所以只有**两种**口吻，而纪律是：

| 这段话在说什么 | 说话人是谁 | 去哪 |
|---|---|---|
| 我现在遇到了 X ／ **如果遇到 X，我该怎么办** | **当事人** | `nodes/`（判断点）——**走正常检索** |
| 有没有人做过 X | **找例子的人** | `nodes/`（案例） |

**教训**：我差点把一个**已有的入口**（症状检索）包装成一个**新类别**
（"出题口吻"，还要给它单独一张表、单独一个命令）。
**新造一个类别之前，先问一句：这是不是已经有的那个东西换了个说法？**

---

## 十三、第六批：那道题不是案例，而 ① 少了"怎么看"

### 起因是两句话

> 「不，这个不是案例，而是对方举出来的一个随意的例子，是考验如果在这样的情况下应该怎么办」

> 「补怎么确认，**出题其实就是症状啊，如果遇到这种情况怎么办，不用新加**」

第一句定了**性质**：那是一道题，不是一个事实记录。
第二句定了**归属**：那种问法**就是症状口吻**，走正常检索就行，**不需要新类别**。

**两条合起来，本批做了三件事：**
删掉误建的案例节点（决议一）、把模板缺的「怎么测」补上（决议二）、
把内容缺的 8 节「怎么确认」补上（决议三）。

### 决议一：删掉 `case-0006`

`case-0006` 是上一批按"案例位"建的（五段骨架、正文一字未填、`source` 两项留空）。
现在确认是举例，**已删除**。

**理由一句话：案例不能由举例生成。**
留一个空壳案例位，等于给「看起来有案例」留了个假证据——
而本产品的红线正是**不代填真实内容**。空壳比没有更坏，因为它骗的是**读结构的人**。

**那么这道题该去哪？答案是：哪也不用新去。**

「如果遇到这种情况怎么办」**就是症状口吻**，它本来就该走正常检索：

```
python tools/find_path.py --problem "我基于 verl 跑 Qwen3.5 的 GRPO，跑到 200 步 reward 曲线崩了，审计发现有一半 rollout 推对了却给了 0 分"
```

→ 0.45 → `judge-0009`。**它不需要新的家，因为它从来没有从系统里掉出去过。**

它要做的只有一件事：**作为一条用例进 `SELF_TEST`**，在 `说明` 里标明它是外部来的。

|  | `nodes/` | `SELF_TEST` |
|---|---|---|
| 装什么 | 知识（可被引用的事实/判断） | **用例**（"这句话该被送到哪、该看到什么"） |
| 谁提供 | 外部来源 / 使用者 | 自己编的（回归）**+ 外部来的（覆盖）** |
| 形态 | Markdown + front matter | **可执行的断言** |
| 能不能编 | **不能**（案例尤其不能） | **能——它就是拿来撞的** |

**我一度做错的地方**：给它单开了一张 `SCENARIO_TEST` 表和一个 `--scenario` 命令。
那是**把一个已有的入口包装成了新类别**——使用者指出来了，已并回 `SELF_TEST`。

### 决议二：① 补上"怎么测"——一个测不出来的变量等于没给

**这是同一道题撞出来的第二个缺口。**

那道题的后半句是「审计到有 50% 的 rollout 推对了却给了 0 分」。
系统当时能说出**「假阴性率」这个名字**，却**不说这个数怎么得到**——
**而 `judge-0009` 的正文里明明写着四步怎么测。**

**原因**：路径模板的 ① 只取 `变量：` 行 + 带数值的判据行，
**不收正文的「怎么确认」节**。而那一节恰恰是判断点最有用的一半。

**修法**：① 从两段改成三段——

```
① 先看什么（变量 → 怎么测 → 判据）
    reward 曲线的走向、这批样本的假阴性率、截断样本占 batch 的比例、……
    怎么测这些量（本节点正文给的步骤）：
      1. 逐条读推理过程，人工判它该不该算对
      2. 和它实际拿到的分对比
      3. 统计「推理对但给了 0」的比例——这就是假阴性率
      4. 再看这些假阴性样本有什么共同点（长度顶到上限？格式少了一行？）
```

**实现**：`_howto_steps()` 抠正文里标题命中
`怎么确认 / 怎么测 / 怎么查 / 如何确认 / 怎么判断` 的 `## ` 小节，
只收 `1. …` / `- …` 形式的条目——**散文段落不收**，因为 ① 是清单不是摘要。

**为什么不改字段**：这是**读取**的问题，不是**存储**的问题。
节点正文里本来就有那一节，缺的是模板没去读它。**动 schema 是错的修法。**

### 决议三：给其余 8 个判断点**补上**「怎么确认」节

**修完模板只解决了一半。** 实测数了一下：

```
judge-0001  步骤数=0      judge-0002  步骤数=0      judge-0003  步骤数=0
judge-0004  步骤数=0      judge-0005  步骤数=0      judge-0006  步骤数=0
judge-0007  步骤数=0      judge-0008  步骤数=0      judge-0009  步骤数=4
```

→ **9 个判断点里只有 1 个写了那一节。** 模板通了，内容还是空的。

**所以补了 8 节**（每个 3–4 步，共 28 步）。**三条纪律：**

1. **每一步都要能回到来源。** 来源给了方法的（judge-0003 的 R1-Zero 长度跳变、
   judge-0006 的日志字段、judge-0007 的前 36 步拟合、judge-0008 的协方差分位）
   → 直接写，并标出处。
2. **来源没给方法的，要标出来源强度。** 例如 judge-0001 第 3 步、
   judge-0002 第 1 步——那是我**把论文的机制读出来的**，
   **不是论文给的日志字段**。这一条必须在正文里写明白，不能混在"论文说的"里。
3. **判断点里也有"不是排查题"的。** `judge-0005`（KL 项该不该开）是**选型题**，
   它的「怎么确认」不是"怎么测一个量"，而是"**先确认你手上是哪种实现**"。
   → **不硬套同一个形状。**

**⚠️ 顺手撞出一个新 bug 并修掉**：`_howto_steps()` 只收 `^\d+\.` 开头的行，
所以**一个跨两行写的步骤，后半句会被静默丢掉**——
路径上看起来完整，其实断在半句话上。实测中了 2 处（judge-0005、judge-0006）。

→ 加了一条不变量：**「怎么确认」的每一步必须以正常标点收尾**，
结尾是 `，：、（` 这类一律 FAIL。实测负例：
把 judge-0005 第 1 步改回跨行 → `[FAIL] …有一步骤像是被换行截断了（结尾是「，」）`。

### 实测：这道题的完整走查

```
$ python tools/find_path.py --problem "我基于 verl 跑 Qwen3.5 的 GRPO，跑到 200 步 reward 曲线崩了，审计发现有一半 rollout 推对了却给了 0 分"

匹配：0.45  →  judge-0009 · reward 不涨或崩了，先查哪一边（判断点）

①  先看什么（变量 → 怎么测 → 判据）
    reward 曲线的走向、这批样本的假阴性率、截断样本占 batch 的比例、进训练的组里奖励方差为 0 的比例
    怎么测这些量（本节点正文给的步骤）：
      1. 逐条读推理过程，人工判它该不该算对
      2. 和它实际拿到的分对比
      3. 统计「推理对但给了 0」的比例——这就是假阴性率
      4. 再看这些假阴性样本有什么共同点（长度顶到上限？格式少了一行？）
②  con-0006 可验证奖励（RLVR）与规则奖励（直连）
③  （未登记例外）        ④  （未登记议题）
⑤  case-0003 [经概念·弱]
⑥  该去看哪一条（分诊支路）：judge-0004 / judge-0002 / judge-0001 / judge-0006
```

**这就是「遇到这种情况怎么办」的答案**——用的就是普通检索命令，没有额外入口。

### 断言是可执行的，而且实测过负例

`SELF_TEST` 的少数条目多带**第 4 个元素**——
**断言的不只是入口，还有路径里该出现的内容**：

```python
("我基于 verl 跑 Qwen3.5 的 GRPO，跑到 200 步 reward 曲线崩了，审计发现有一半 rollout 推对了却给了 0 分",
 "judge-0009", "外部原话 + 断言（要求给出测法，不只是量名）",
 {"①": ["假阴性率", "怎么测"], "②": ["con-0006"], "⑥": ["judge-0004"]})
```

**⚠️ 只有一张表，两种条目。** 大多数条目只断入口（回归），
少数带断言（覆盖）。**不为外部用例另开一层**——理由见决议一。

实测的负例（否则这条纪律只是愿望）：

| 故意破坏 | 结果 |
|---|---|
| 断言里塞一个不会出现的词 | `[FAIL] …的 ① 步里没出现「绝不存在的词」` |
| 断言一个本来就空的步骤 | `[FAIL] …的 ③ 步是空的——用例走到了这一步，路径却没内容` |
| 把期望入口改成错的 | `[FAIL] …期望 judge-0004，实得 judge-0009（最高分 judge-0009 0.45）` |
| 「怎么确认」写成跨两行 | `[FAIL] judge-0005 …有一步骤像是被换行截断了（结尾是「，」）` |

### 一条纪律：**用例的"外来身份"要写在说明里**

为什么：**自洽 ≠ 覆盖**。一条自己编的用例混在里面，你分不清这一表全绿
到底证明了什么。所以外部来的用例，`说明` 一栏要写明它是外部的。

⚠️ **但这件事不需要一个字段、更不需要一张表。**
我一度给它做了 `来历` 字段 + 独立表 + 独立命令——那是**过度设计**。
写在说明里就够了。**能不加结构就不加结构。**

### 本批不改的东西

- **节点数 32 → 31**（只删了一个，没加）；**`relations` 只动了一处**（`judge-0004` 去掉 `case-0006`）
- **`judge-0009` 一个字没改**——它的内容本来就够，缺的是模板没去读
- **上游 R9 的盲区只记录，不改**
- **`evidence_status` 全部仍为 `未验证`**
- **没有新增任何外部来源**——补的 8 节全部取自各节点**已有的** `source.ref`
