# rl-scaffold · 缺口清单（2026-09-23 第六批）

<!-- Copyright 2026 AIC-123 · SPDX-License-Identifier: Apache-2.0 -->

> ⚠️ **本文件只产出清单。没有动任何节点、没有加任何 cue、没有改任何 `relations`。**
> 哪几条该补由人决定——**这一步刻意不自动化。**

## 一、这份清单是怎么来的

**方向反过来了。** 前五批是「先把来源读成节点，再从节点里提炼处境句」——
而 `PLAN.md` §一 自己定的第一条纪律是：

> **索引规格必须先于内容映射**（写不出情境句的内容，根本不该被收录）

顺序反了的后果：**只长出了"论文会写的那部分"**。
论文写的是「我们做了什么」，不是「你该先想什么」。

所以这一批**反过来走**：

```
先写 39 句当事人原话  →  全部丢进匹配器  →  没落下去的那几句就是缺口
```

**语料在 `tools/probe_gaps.py`**（那个文件是**采集**工具；`find_path.py --check` 是**验收**工具，
两者方向相反：一个问"我还差什么"，一个问"我做对了吗"）。

跑法：

```bash
python tools/probe_gaps.py          # 全量
python tools/probe_gaps.py --miss   # 只看缺口
```

## 二、判定只有四种，都不用拍数

| 判定 | 含义 | 是缺口吗 |
|---|---|---|
| `HIT` | 正常命中 | 否 |
| `MISS` | 没匹配上（分数没过阈值） | **是** |
| `NOCUE` | 落在判断点上，但**一条 cue 都没重合** | **是**（缺的是一句处境） |
| `NONJUDGE` | 落在**非判断点**上（概念/例外/案例） | **不算错**，但要人看一眼 |

`NONJUDGE` 不算错的理由：`PLAN.md` §五 的「面试者」入口要的就是概念节点。
列出来只是为了让人看——**里面混着"落对了"和"落错了"两种，而分清它们需要眼睛。**

> 早先还有一档「分数边缘（< 0.45）」。**实测一份语料下来一次都没触发，已删掉**——
> 一个从不触发的阈值就是拍脑袋。

## 三、总账

**补之前（2026-09-23 第六批，31 节点时）**：

```
语料 39 句（正例 35 + 越界负例 4）

  正常命中        7
  缺口           23   ← 没匹配上 23 / 落在判断点上却无 cue 重合 0
  落在非判断点    5   （不算错）
  越界负例        4 句，其中 4 句正确落不下去  ✅
```

**补之后（第七批，35 节点时）**：

```
  正常命中       14
  缺口           16   ← 全在 C 选型 / D 超参 / E 数据 / F 评测
  落在非判断点    5
  越界负例        4 句，其中 4 句正确落不下去  ✅
```

> ⚠️ **但这个 7 → 14 不能当成"覆盖变好了"。** 见 §四 B 节里那段：
> 新节点的 `cues` 里**写了语料原句**，所以命中是构造出来的。
> **真正那把尺子是 `probe_gaps.py --held`（同义改写命中率），现在报 9/17。**

**先说好消息两条：**

1. **`A 排查者` 对照组 4 句全中，分数 1.13–1.72。**
   ⚠️ **但这一组也是构造的**——那 4 句和 `judge-0001`/`0009`/`0006`/`0002` 的 cue
   **几乎逐字相同**。所以它证明的是"我把像的话写进了 cues"，**不是"改个说法也找得到"**。
   （这一条是第七批才看出来的，第六批当时的结论下早了。）
2. **4 句越界负例全部正确落不下去。** 说明结构**没有收得太宽**——这一条**没被推翻**。

**再看一个关键数字**：那 23 句缺口的最高分普遍落在 **0.11–0.24**（阈值 0.30）。
**它们不是"差一点"，是"差得远"。**
→ 而第七批发现：**"差得远"的原因里，有一部分是匹配算法不认同义改写**，
不全是"结构里没有那些内容"。两条要分开算。

## 四、逐类清单

### A 排查者 —— **满的**（0 缺口）

| 原话 | 结果 |
|---|---|
| 日志里 entropy 一路往下掉，我不知道要不要停 | 1.72 → `judge-0001` |
| reward 曲线跑到一半崩了，不知道先查哪里 | 1.72 → `judge-0009` |
| actor/grad_norm 一直往上爬，调学习率也没用 | 1.43 → `judge-0006` |
| 一批 prompt 奖励全是 1，感觉没贡献梯度 | 1.13 → `judge-0002` |

**这一组是刻意放的对照组。** 它全中，后面的缺口才站得住。

### B 事前 · 判分规则设计 —— ✅ **本批已补（2026-09-23 第七批）**

> ⚠️ **2026-09-23 更正**：本节原写「一整块缺（7 句缺 6）」，**两处都数错了**——
> 表格实为 **8 行、7 行未命中**。§三 的总账（缺口 23）本来就是按 B=7 算的，
> 所以**总账是对的，只有这个表头错**。见 `AUDIT.md` §三。

**原状（补之前）**：

| 原话 | 最高分 | 落到了哪 |
|---|---|---|
| 我打算用规则奖励，担心模型学会只对格式不对答案 | 0.21 | `con-0006` |
| 我的判分规则用正则匹配，会不会有漏判 | 0.15 | `judge-0004` |
| 我该不该给格式分 | 0.23 | `judge-0005` |
| 奖励里要不要加长度惩罚 | 0.15 | `con-0006` |
| 我想让模型学会一步步想，但不知道奖励怎么给 | 0.24 | `case-0003` |
| 同一个答案有好几种写法，正则只匹配一种，怎么办 | 0.14 | `judge-0008` |
| 要不要给中间步骤打分，还是只看最后答案 | 0.12 | `exc-0002` |
| ~~训练一开始 reward 就冲到 0.99，我怀疑规则太松了~~ | 0.60 ✅ | `judge-0009` |

**7 句里 5 句指向同一块**：判分规则本身怎么设计（会不会被钻空子、鲁棒性、给不给过程分）。

**唯一命中的那句为什么中**：它长得像"排查"（reward 异常）——**这恰好说明结构只认排查口吻。**

### 本批补了什么（第七批）

新增 **4 个判断点**，全部**从既有来源组装**，没有引入任何新来源：

| 新节点 | 治哪几句 | 判据来自 |
|---|---|---|
| `judge-0010` 判分规则写好了，怎么知道它会漏判 | 判分规则用正则会不会漏判 / 同一个答案多种写法 | R1 §2.2、DeepSeekMath §5.2.3 |
| `judge-0011` 格式分该不该给 | 我该不该给格式分 / 担心只对格式不对答案 | R1 §2.2、Table 3 |
| `judge-0012` 过程分该不该给 | 要不要给中间步骤打分 / 一步步想怎么给奖励 | DeepSeekMath §4.1.3、§5.2.3；R1 §2.2 |
| `judge-0013` 长度惩罚要不要加 | 奖励里要不要加长度惩罚 | DAPO §3.4、§4；Dr. GRPO 附录 F |

### ⚠️ 但下面这件事必须说破：那个「缺口 23 → 16」大半是**循环论证**

**我把语料原句直接写进了 `cues`。** 于是它们当然命中（分数全是 1.00）。
**那不是"接住了"，那是把测试集喂成了训练集。**

**实测证据**（`python tools/probe_gaps.py --held`）：

| 时点 | 留出集（与 cues 不同的说法） | 命中 |
|---|---|---|
| 刚补完节点、未加 cue | 9 句 | **4/9** |
| 按自然说法补完 cue 之后，**换一套全新句子** | 9 句 | **1/9** |
| 两套并起来 | **17 句** | **9/17** |

**→ 结论不是"缺口补上了"，是"检索的瓶颈不在内容量，在匹配算法"。**
中文走的是**字符二元组重合**，需要**字面共享**；真人换一套词说同一件事，就落不下去。

→ **不要用"往 cues 里多抄几句"压这个数**——那还是把测试集喂成训练集。
要动就得动匹配算法，那是 `D-04` 级别的事，见 `DECISIONS.md`。


### C 选型 —— **6 句里 4 句缺**（另 2 句落在了非判断点上）

| 原话 | 结果 |
|---|---|
| 我要不要上 RL，还是继续 SFT 就够了 | **✗ 0.22** |
| 手上有 8 张卡，该跑多大的模型 | **✗ 0.15** |
| 该用现成框架还是自己写训练循环 | **✗ 0.12** |
| 我该选 verl 还是 OpenRLHF | **✗ 0.17** |
| GRPO 和 PPO 我该选哪个 | 0.49 → `exc-0001` ⚠️ 落错（见 G 节） |
| 我显存不够，能不能只用 GRPO 不要 critic | 0.85 → `con-0002` ✅ 落对了 |

⚠️ **`PLAN.md` §五 明确把「选型者」列为四个入口之一，而它现在基本接不住。**

### D 超参 —— **4 句里 3 句缺**，但**可能是故意不收的**

| 原话 | 结果 |
|---|---|
| 学习率设多少合适 | **✗ 0.24** |
| batch size 该开多大 | **✗ 0.14** |
| 训练多少步算够 | **✗ 0.17** |
| 每个 prompt 采样几条比较合适 | 0.70 → `judge-0002` ⚠️ 落错 |

⚠️ `PLAN.md` §一 写着本产品**不是**「超参推荐表」。
**所以这一组大概率是"不该补"，不是"漏了"。** 需要拍板。

⚠️ 那句 0.70 的也是**落错**（我人工标的）：它是个超参问题，
却落到了 `judge-0002`（**一组全对或全错，这一组是不是白跑了**）——
靠的是"采样"「组」这几个词。**分数越高越危险：0.70 看起来完全可信。**

### E 数据 / 任务构造 —— 缺，**一半是处境一半是指南**（4 句全缺）

| 原话 | 最高分 | 我的读法（**建议，不是决定**） |
|---|---|---|
| 我的训练集只有几百条，够不够 | 0.21 | 处境 |
| 题目太简单了模型全答对，要不要换难一点的 | 0.12 | 处境（和 `judge-0002` 沾边） |
| 训练数据要不要去重 | 0.24 | 偏指南 |
| 该用什么难度的题 | 0.11 | 偏指南 |

### F 评测 / 上线 —— 缺，**一半是症状一半是指南**（5 句全缺）

| 原话 | 最高分 | 我的读法（**建议，不是决定**） |
|---|---|---|
| 训练集分数涨了但测试集没涨，是不是过拟合了 | 0.11 | 处境 |
| 训完的模型和基座比好像变笨了 | 0.15 | **症状**（和 `judge-0005` KL 沾边） |
| 我该用什么评测集 | 0.11 | 偏指南 |
| 训完之后要不要再做一轮 SFT | 0.20 | 偏指南 |
| 模型训完开始输出重复内容 | 0.20 | **症状**（和熵坍缩沾边） |

### G 面试 —— **0 缺口**（4 句全部落下去了）

| 原话 | 落到了哪 |
|---|---|
| 为什么 GRPO 不需要 critic | 1.45 → `con-0002` ✅ |
| 组内归一化是怎么算的 | 0.33 → `con-0001` ✅ |
| GRPO 和 PPO 的区别是什么 | 0.58 → `exc-0001` ⚠️ 落错 |
| 什么是 reward hacking | 0.57 → `judge-0004` ⚠️ 落错 |

**⚠️ 下面这三句是我人工标出来的"落错了"**（工具判不出来，因为它没有标准答案）：

> ⚠️ **2026-09-23 更正**：原写「G 面试 —— 0 缺口（**4 句全部落下去了**）」+「下面**三句**」，
> **两处都不准**：G 这 4 句里**只有 2 句**标了落错（下面表的前两行），
> 第 3 句「GRPO 和 PPO 我该选哪个」**属 C 选型组**，被误算进 G 了。
> 所以准确说法是：**G 组 4 句里 2 句落错；另 1 句在 C 组。** 见 `AUDIT.md` §三。

| 原话 | 落到了 | 靠什么落下来的 | 属哪组 |
|---|---|---|---|
| GRPO 和 PPO 的区别是什么 | `exc-0001`（例外：**去掉标准差项**的代价） | `grpo` / `ppo` 两个通用标识符 | G |
| 什么是 reward hacking | `judge-0004`（**被截断的样本**） | 一个词：`reward` | G |
| GRPO 和 PPO 我该选哪个 | `exc-0001`（同上） | 同上 | **C**（不在 G 的 4 句里） |

> **这三句是"看起来通了"。** 而 `judge-0004` 那句和 §十二 记下的
> `judge-0009` 那次 `cue=0.000` 是**同一个病**：让一个不构成证据的词替读者选了节点。
>
> ⚠️ **工具只抓得住结构性可疑（无 cue 重合），抓不住语义性落错。**
> 这三条是**人工判断，不冒充自动结论**——所以它们不在上面的 `NOCUE` 计数里。

### H 越界 —— **全对**（4 句全部正确落不下去）

| 原话 | 最高分 |
|---|---|
| 我的显卡坏了怎么办 | 0.13 |
| 公司不给我批算力 | 0.11 |
| 今天天气怎么样 | 0.09 |
| 帮我写一首诗 | 0.00 |

## 五、从清单里能读出来的三件事（**不是决定**）

1. **不是"事前缺一块"，是"四个入口里两个基本没铺"。**
   `PLAN.md` §五 定了四个入口：排查者（**满**）、选型者（**基本空**）、
   面试者（**0 缺口**；4 句里 2 句落错，另有 1 句在 C 组）、越界（**全对**）。
   **选型者和"事前"高度重叠**——都是"还没开跑 / 刚开跑"的问题。

   > ⚠️ **2026-09-23 更正**：原写「有 3 句落错」，把 C 组那句算进面试者了。
   > 见上面 G 节的更正框。

   > ⚠️ **2026-09-23 补一条只跟"读法"有关的更正**：本节第 2、3 条把
   > `D 超参` / 指南向句子判成「**可能是故意不收的**、需要拍板」，
   > 而第 3 条又说「事前是内容缺口」——**同一批东西，两条结论**。
   > 现在定案：**不分"处境 / 指南"，指南式问法就是处境的一种**（提取已交代的
   > 条件与约束后匹配，不添加用户没说的事实）。所以第 2 条那个"故意不收"的
   > 前提**不成立**，`D 超参` 等仍按普通缺口看待。
   > **这件事已定，不再重开**——详见 `AUDIT.md` §四 与 `DECISIONS.md`。

2. **有一组缺口需要分清"缺来源"还是"缺组装"。**
   `D 超参` 和 `E/F` 里的句子，**来源状况分两类**：
   ① 库里**已有来源、只是没组装**的（如"判分规则会不会漏判""要不要给过程分"）；
   ② **一条来源都没有、必须先联网找**的（如"学习率设多少""verl 还是 OpenRLHF"）。
   → **缺口清单不等于待办清单。** ② 那一类不先找来源就动笔，就是编。

3. **「事前」那 5 句有同一个形状**：都不是排查，是**"我打算这么做，行不行"**。
   而结构里能装下这种形状的判断点**只有 `judge-0004` / `judge-0005` 两条**。
   → 说明**框架装得下，只是没装**。这是内容缺口，不是框架错。
   （⚠️ 旧版这里写的是"唯一装得下这种形状的是…"，与第 2 条并列时读起来像
   "结构不支持"；准确说法是**支持但没铺**。已改。）

## 六、明确没做的事

- ❌ 没加任何节点、没加任何 cue、没改任何 `relations`
- ❌ 没把语料里的句子塞进 `SELF_TEST`——**因为现在塞进去 `--check` 会失败**。
  它们是**待办**，不是**验收项**；等节点补上了再移过去
- ❌ 没有对"该补哪几条"做任何决定

**语料只加不减。** 下次补之前，先往 `tools/probe_gaps.py` 的 `CORPUS` 里加句子——
**加句子比加节点便宜，而且加句子才知道该加哪个节点。**
