# RL 工程知识结构 · 步骤计划

<!-- Copyright 2026 AIC-123 · SPDX-License-Identifier: Apache-2.0 -->

> **状态：八轮已执行完毕（2026-09-23）。**
> **36 个节点** / ERROR 0 / WARN 0 / 退出码 0；情境索引 **112 条处境**；**14 条可行路径**；
> 三个 `--check`（校验器自检、视图一致性、检索自检）全部通过；
> 检索自检 **25 条固定用例**（含 4 负例 + 3 组内容断言）+ **1/1 条长口述拆句**。
> 阶段 1–8 全部走通，产出见 `README.md`、`SOURCES.md`、`ADAPT.md`、`AUDIT.md`、`DECISIONS.md`。
>
> **第一批（22 节点）**：偏「是什么 / 怎么做」，来源以**摘要**为主。
> **第二批（+8 节点）**：按 §六 的四个渠道补**诊断类**内容，读到正文，所以带数字的判据集中在这一批。
> **第三批（节点数不变，只加厚）**：把第一批里只读摘要的三篇论文
> （DeepSeekMath / Dr. GRPO / DeepSeek-R1）**补读 HTML 正文**，
> 依赖它们的 8 个节点同步补上正文级内容。记录见 `SOURCES.md` §七。
> ⚠️ 这一轮**暴露了一个流程漏洞**：某节点的 `source.ref` 曾声称章节号，
> 而来源清单写的是「摘要页」——**引用范围超过了实际读到的范围，当时没被拦住**。
> 已登记为欠账（`README.md`「下一批该做什么」第 2 条）。
>
> **第四批（节点数不变，补第 ⑥ 层的「查询」那一半）**：
> 视图层上一轮只做了「排序」，把「查询」当成了过滤框。
> 这一轮加了 `tools/find_path.py`：把**你遇到的问题**换成一条**六步可行路径**。
> 记录见 `ADAPT.md` §十一。
> ⚠️ 这一轮有个反直觉的结果：**30 个节点的 `relations` 一条都没改**——
> 第一版路径模板漏了 `judge → 例外 → 议题` 这条两步链，
> 看起来像"数据缺边"，其实是**走法不对**。先怀疑走法。
>
> **第五批（+2 节点，补上产品自己声明的那个主入口）**：
> 拿一个**外部场景**（对方举出的考验用例）去撞上一轮的检索，撞穿了——
> 「reward 曲线崩了」的 `cue` 重合 **0.000**、`title` 重合 **0.000**。
> 而 `PLAN.md` §一 第一句就写着「真实入口是**『reward 不涨了』**」。
> **第一版 75 条处境里一条都不是它。**
> ⚠️ **自检为什么没抓到**：`SELF_TEST` 的用例都是照着**已有节点**写的，
> 只能证明"已有的入口都能被找到"，**证明不了"入口齐了"**。
> → 新增 `judge-0009`（**分诊**判断点，本结构里唯一 `source.kind: 其他` 的节点）。
> 记录见 `ADAPT.md` §十二。
>
> **第六批（−1 节点，三处结构性修正）**：使用者澄清两件事——
> ① 「那个例子不是案例，是对方举出来考验系统的一道题」；
> ② 「**出题其实就是症状啊**，如果遇到这种情况怎么办，**不用新加**」。
> → **删掉 `case-0006`**（案例不能由举例生成）；**但不为它新开一层**——
> 那种问法就是症状口吻，走正常检索，它只需作为一条**带内容断言的用例**进 `SELF_TEST`。
> ⚠️ 我一度给它单开了 `SCENARIO_TEST` 表与 `--scenario` 命令，**已并回**：
> **把一个已有的入口包装成新类别，是过度设计。**
> → 同一道题还撞出第二个缺口：路径模板的 ① **只给"看什么"，不给"怎么看"**——
> 系统能说出「假阴性率」这个名字，却不说怎么测（而节点正文里写着）。
> ① 改成三段：**变量 → 怎么测 → 判据**。
> → 而**修模板只解决一半**：9 个判断点里只有 1 个写了那一节。
> 于是给其余 **8 个判断点补了「怎么确认」**（共 28 步，全部取自各节点已有来源）。
> 记录见 `ADAPT.md` §十三。
>
> ⚠️ 第一批在 `SOURCES.md` 里写下的「诊断类只有二手来源」结论**是错的，已在原处更正并保留**——
> 错因是搜索词（搜「避坑」而非搜「失败模式」），不是来源少。
>
> **第七批（+4 节点，一个绕圈根因已止血）**：分两段。
> **前一段是"修"**——按 `AUDIT.md` 做了批 A（机制 5 项，每项验过负例）/ 批 B（10 条文档不实）/
> 批 D（新增 `DECISIONS.md`：9 条已定案 + 8 条规则 + 开工前必读顺序）。
> 见 `AUDIT.md` §六·补。
>
> **后一段是"补"**——按 D-09 的 C1：`B 事前 · 判分规则设计` **8 句缺 7**，
> 新增 **4 个判断点、零新来源**（`judge-0010` 判分覆盖 / `judge-0011` 格式分 /
> `judge-0012` 过程分 / `judge-0013` 长度惩罚）。**C2 `选型` 未做。**
>
> ⚠️ **但本批最重要的产出是一条更正**：补完之后缺口从 23 掉到 16，
> **那个掉幅大半是假的**——我把语料原句写进了 `cues`，命中是构造出来的。
> 改用**留出集**（与 `cues` 不同的说法）测同义改写，**命中率 9/17**。
> → **检索的瓶颈不在内容量，在匹配算法**（中文走字符二元组，需要字面共享）。
> 两个数从此分开报，见 `DECISIONS.md` **D-10 / R-8**，尺子在 `tools/probe_gaps.py --held`。
>
> **已做**：`git init` + 首次提交（`0410c20`）。**未做**：命名、发布（等你拍板）。
> `C2 选型` **部分完成**：补了「要不要上 RL」；「框架/规模选型」按边界不收，见 `SOURCES.md` §十一。


> 覆盖判据（§五）里只走通了**主入口（排查者）**，其余三个入口"有路"。
>
> **与 Scaffold 的关系**：Scaffold（`ks-demo`，v0.0.3，`f140642`）在这里是**上游规格**——六层结构、
> 双索引、R1–R10 校验器被**引用**，**不被修改**。本产品是独立的新产品，
> 不碰 `ks-demo` 的任何文件（已实测核对：`git status --porcelain` 为 0 行；
> `validator/` 与 `schema/` 与上游 `diff -q` 逐字节一致）。
>
> 目录名 `rl-scaffold` 是**占位名**，等你命名（改名成本 = 一次 `mv` + 一处 sed）。

---

## 一、先回答一个问题：这个产品靠什么被找到

Scaffold 的立场是「交付结构，不交付内容」，且明确**不提前做检索**。
那么一个 RL 知识产品，用户是怎么"首值 ≤5 分钟"的？

**答：靠情境索引。**

一个 RL 工程师的真实入口不是「我想学 GRPO」，而是**「reward 不涨了」**。
所以本产品的核心不是概念词条，而是**「你现在的处境 → 该看哪几条」**。

> ⚠️ **2026-09-23 第五批补的账**：上面这句写对了，**但结构里没有兑现它**——
> 第一版 75 条处境里，**一条都不是「reward 不涨了」**。
> 八个判断点覆盖了熵、grad_norm、长度、KL、全对组、截断、token 干预，
> 偏偏漏了最上位的那个症状。是拿一次真实排查对话撞出来的，见 `ADAPT.md` §十二。
>
> **这条判断现在由 `judge-0009` 兑现。**

这条判断决定了后面所有步骤的**顺序**：**索引规格必须先于内容映射**
（写不出情境句的内容，根本不该被收录）。

**它不是**：教材、论文导读、超参推荐表、任何形式的"最佳实践排行榜"。

---

## 二、RL 领域对结构的四个压力点（已联网核实）

| # | 压力点 | 证据 | 影响 |
|---|---|---|---|
| 1 | **知识形态是「诊断式」而非「陈述式」** | 公共讨论集中在 reward 不涨 / 长度爆炸 / 熵坍缩 / reward hacking / 信用分配 / 致命三元组 | Scaffold 九类节点里最接近的是「判断点」与「案例」，**能否承载「现象→原因→修法」需实测** |
| 2 | **来源质量断层** ⚠️ **第二批已更正：这一条判断错了** | ~~一手来源覆盖"是什么/怎么做"；诊断类知识几乎全在二手博客~~ → 实为：**诊断类一手来源存在且更硬**（自带判据数值），当时搜不到是因为**搜索词错了** | 原判断会让 `source.kind` 被迫大量落「其他」；更正后**枚举一个字没改**，二手来源约定至今未触发。详见 `SOURCES.md` §3.3 |
| 3 | **时效性极强且可观测** | RLHF Book 自己的 changelog：2025-01 v1 → 2025-06 v1.1 → 2026-02 v2，一年多**大改版并重组章节** | 需要"这条基于哪个版本/时间"的约定；Scaffold 的 `scope`（适用范围，必须同时写清不适用）可承载，但**要定写法** |
| 4 | **分歧活跃且是「条件性」分歧** | PPO vs GRPO、要不要 critic、rule-based vs model-based reward、length bias 修不修——多是"在什么规模/任务下谁对" | 需要 `evidence_status: 有争议` + `条件` 类型配合，且**只列不裁** |

**结论**：步骤不能是「建目录 → 填内容」。必须先做**索引规格**与**结构压力测试**。

---

## 三、步骤（8 阶段）

### 阶段 1 · 边界与判据（先定"不做什么"）

1.1 **用户画像**：三类使用者各自的入口
   - 排查者：手上有个跑坏的 run（**主入口**）
   - 选型者：要不要上 RL、上哪种
   - 面试者：被问到某个概念，需要一句话 + 一个可查出处
1.2 **边界声明**：明确不收什么
   - 不收公司私有配置、未公开实验结果
   - 不收"哪个更好"的裁决（只列分歧）
   - 不收需要付费墙才能核验的来源
1.3 **成功判据**（可实测，不是口号）
   - 首值：给定一句现象描述，能否在索引里定位到节点
   - 越界：遇到"该找真人"的问题时，产品是否正确停下

**产出**：`README.md` 的「它是什么 / 它不是什么」两节初稿

### 阶段 2 · 索引规格（**先于内容**，这是硬规矩 4 的落地）

2.1 **情境索引（`cues`）写法约定**
   - 句式：**以使用者的处境为准**，不以知识点为准
     - ✅「reward 曲线前期涨、后期平」　❌「GRPO 的奖励设计」
   - 每条节点 1–3 条 cues，必须能被"我现在遇到 X"直接命中
2.2 **概念索引（`title`）命名约定**：一名一义，别名进 `aliases`
2.3 **排序键声明**：只按文件路径。**来源、机构、置信度一律不进排序**
2.4 **时效标注约定**：每条工程性断言在 `scope` 里写清"基于哪个版本/什么配置下成立"
2.5 **索引页形态**：静态、可 git diff 的 Markdown。
   ~~**不做检索**——那是 Scaffold 明确不提前做的事~~
   → ✅ **第四批已更正**：Scaffold 对第 ⑥ 层的原文是「**查询**/排序规格」，
   **「查询」两个字一直在那儿**，第一轮把"查询"当成了"过滤框"。
   过滤要求你先知道关键词，**查询要求你描述处境**——两者不是一回事。
   落地为 `tools/find_path.py`（确定性匹配 → 六步可行路径），见 `ADAPT.md` §十一。
   **仍然是静态的**：无服务端、无索引库、无模型，可离线跑、可 git diff

**产出**：`索引规格.md`

### 阶段 3 · 结构压力测试（拿真内容撞九类节点）

3.1 **取样**：从三类来源各取若干真实内容形态
   - 论文断言（GRPO 原文的机制陈述）
   - 官方文档配置（verl 的配置项与含义）
   - 二手诊断表（现象→原因→修法）
3.2 **逐条映射**到 Scaffold 九类节点（概念/判断点/条件/例外/议题/立场/论据/断言/案例）
3.3 **记录装不下的**，每个缺口三选一：
   - (a) 扩类型 —— **不做**（会改 Scaffold）
   - (b) 组合映射 —— 用现有类型的组合承载
   - (c) 明确不收录 —— 写进「我们不规定什么」
3.4 **重点验证**：诊断表能否用「判断点（看哪几个变量）+ 例外（什么情况下不成立）+ 案例（具体那次）」组合承载

**产出**：`结构适配决议.md`（每个缺口 + 处理方式 + 理由）

### 阶段 4 · 来源预检（**先盘来源，再决定填什么**）

4.1 列候选来源，标注**一手 / 二手**
4.2 实测每条的可核验程度：能不能给出**稳定标识**（arXiv 号 / DOI / 官方文档 URL + 章节）
4.3 **定收录门槛**：只有二手来源的内容收不收？
   - 倾向：**收**，但 `source.kind: 其他` + 在 `notes` 里**自曝是二手**
   - 理由：诊断类知识只有二手来源；不收就等于放弃产品的主入口
   - 代价：必须在 README 里把这个断层**说在明处**
4.4 **二手来源的处理约定**（不动 Scaffold 的 schema）：
   在本产品 README 里定一条约定——**二手来源必须在 `notes` 首行自曝**，
   且**永不标 `已确立`**

**产出**：`来源清单.md`

### 阶段 5 · 规模预算与覆盖判据

5.1 **节点数上限**：首版 ≤ 60 条（能交付比能覆盖重要）
5.2 **覆盖判据**：用阶段 3 的映射表当 checklist，覆盖到哪一层算"够"
5.3 **不追求完备**的显式声明

**产出**：并入 `README.md`

### 阶段 6 · 立骨架（**只立骨架，不填内容**）

6.1 按阶段 3 的映射建节点，`id` 按 Scaffold 的前缀规则
6.2 `title` / `cues` / `scope` 三项按阶段 2 的规格写——**这三项属于骨架，不属于内容**
6.3 ⚠️ **骨架节点的 `filled_by` 写什么？** —— 这是 R8 判据未定案的直接后果，
   见文末「待你拍板」
6.4 跑校验：预期 **ERROR 0**

**产出**：节点骨架 + 校验报告

### 阶段 7 · 填内容（⚠️ 红线区）

7.1 **只填有一手来源的断言**；二手来源的内容进 `notes` 并自曝
7.2 每条必带 `source.ref`（稳定标识）+ `source.kind`
7.3 `evidence_status` **一律 `未验证`**
   —— Scaffold 的规矩：`已验证` 只能由外部授予，**模型填的一律不能是已验证**
7.4 `filled_by: 模型(<具体模型名>)` —— **不隐藏、不模糊**
7.5 **分歧只列不裁**：写清"谁主张什么 + 在什么条件下"

**填充顺序**（不等于层序，按可核验性排）：
`词汇/结构（可引用原话）` → `案例（可查的硬事实）` → `分歧（谁主张什么）` → `归纳性断言`

### 阶段 8 · 校验、索引页与文档

8.1 跑 Scaffold 的四条命令，基线必须可复现
8.2 生成**情境索引页**（按 cues 分组，静态 Markdown，可 git diff）
8.3 生成**待填清单**——把没填的显式列出来，而不是藏起来
8.4 `README.md` 定稿，**置信度声明放在最前面**
8.5 许可与版权（Apache-2.0，与 Scaffold 一致）
8.6 ✅ **第四批补上**：生成**可行路径**（六步），并让**检索**可用——
    `tools/find_path.py`（按需求检索 + 路径模板 + 检索自检）
    与 `index/view.html` 里的「按需求检索」一节。
    **关键纪律**：模糊匹配**只有一处实现**，HTML 里不重复实现打分

---

## 四、迭代记录（两轮自我批判）

### 第一版（直觉版，已废弃）

> 建目录 → 定节点类型 → 立骨架 → 填内容 → 标来源 → 校验 → 做视图 → 写 README → 发布

**七处问题**：

1. **没有「结构压力测试」** → 会照搬 Scaffold 九类节点，填到一半才发现承载不了诊断表
2. **没有「来源预检」** → 填到一半才发现某类内容没有可靠来源
3. **视图放在最后** → 错。Scaffold 的立场是视图层是**规格**，规格决定 cues 怎么写
   （**已落地**：见 `README.md` 的「视图层」一节——规格可见/可改/可版本化，
   两个视图同序由 `tools/render_view.py --check` 强制）
4. **没有「规模上限」** → 无界会导致永远填不完，永远交付不了
5. **没有「边界声明」** → 读者会以为这是权威知识
6. **没有「时效策略」** → RL 变化快，节点会静默腐烂
7. **填充顺序错** → 先填归纳性断言，再找案例，会导致断言找不到落点

### 第二版（本版）

- 加**阶段 1 边界与判据**（问题 5）
- 加**阶段 2 索引规格**并**提到内容映射之前**（问题 3）
- 加**阶段 3 结构压力测试**（问题 1）
- 加**阶段 4 来源预检**（问题 2）
- 加**阶段 5 规模预算**（问题 4）
- **阶段 2.4 时效标注约定**（问题 6）
- **阶段 7 明确填充顺序**：按可核验性排（问题 7）

### 第三版（还差什么？——留给你判断）

我认为已经够覆盖，但有三处**我故意没做**，理由如下：

| 没做的 | 理由 | 现状 |
|---|---|---|
| ~~**不做检索/查询工具**~~ | ~~Scaffold 明确"不提前做检索"~~ | ⚠️ **这条判断错了**。Scaffold 的原文是「**查询**/排序规格」——查询本来就在第 ⑥ 层里。第四批补上了 `tools/find_path.py`，见 `ADAPT.md` §十一 |
| **不做 Web UI** | 文本优先是既定技术审美 | 维持。`view.html` 是单文件静态渲染，不是应用 |
| **不扩节点类型** | 扩类型要改 Scaffold 的 schema —— 你说了不要在 v0.0.3 上改 | 维持。四批下来一次都没需要扩类型 |

**第四批额外定下的一条**：**不用模型做匹配**。
理由见 `ADAPT.md` §十一——模型提名入口是"不可核对的声称"，
模型给候选打分是"让不透明的东西进排序"，两条都撞本产品的红线。

如果这三条里有你想要的，阶段 8 要重排。

---

## 五、覆盖判据（怎么算"够覆盖这个领域"）

不是"节点数够多"，而是**四个入口都能走通**：

| 入口 | 判据 | 怎么验 |
|---|---|---|
| 排查者 | 给定 10 句真实现象描述，能在索引里定位到 ≥8 条相关节点 | ✅ **第四批可自动化了**：`tools/find_path.py --check` 的 `SELF_TEST` 表就是这个判据的可执行形态（**25 条：21 正例 + 4 负例，其中 1 条带 3 组内容断言**——此处曾是"10 条正例 + 2 条负例"、后改"18 条"，都已是旧数，**以 `--check` 的自述为准**） |
| 选型者 | 「要不要上 RL」这条路径上有节点可走 | 手工走一遍 |
| 面试者 | 每个被问到的概念都能给出一句话 + 一个可查出处 | 抽样 10 个 |
| 越界 | 遇到"该找真人"的问题时能正确停下 | ✅ **已有负例**：`--problem "今天天气怎么样"` 必须返回「没匹配上」并退出码 2 |

**"够"是相对的**：首版只要求**主入口（排查者）走通**，其余三个入口"有路"即可。

**第四批带来的一条认识**：覆盖判据里只有"排查者"这一条是**可自动化的**，
而它恰好是主入口。→ **可自动化的那一条先钉死，其余三条继续靠手工。**
把不可自动化的判据写成 checklist，比把它写成一个永远跑不起来的测试诚实。

---

## 六、来源预检

### 第一批的预检（当时是初步的，实际引用的以 `SOURCES.md` 为准）

**一手来源**（可给稳定标识）：

| 来源 | 类型 | 稳定标识 | 备注 |
|---|---|---|---|
| RLHF Book（Nathan Lambert） | 教材 | `rlhfbook.com` / arXiv `2504.12501` / `github.com/natolambert/rlhf-book` | 开放、带 codebase 与 course；覆盖 post-training 全流程 |
| verl 官方文档 | 官方文档 | `verl.readthedocs.io` | 含 perf_tuning 指南 |
| verl 仓库 | 官方文档 | `github.com/volcengine/verl` | 含 examples |
| 火山引擎 veMLP「verl GRPO 最佳实践」 | 官方文档 | `volcengine.com/docs/6459/1463942` | ⚠️ **含平台绑定**（AK/SK、vePFS），最终决定不收录 |
| OpenAI Spinning Up | 教材 | `spinningup.openai.com` | 经典 RL，**非 LLM**，作为背景层 |
| TinyZero | 案例 | `github.com/Jiayi-Pan/TinyZero` | R1-Zero 复现，可查实验 |
| GRPO / DAPO / Dr.GRPO 等论文 | 论文 | arXiv | 待逐篇核 |

**当时记下的二手来源**（**尚未打开核验**）：
- 知乎「大模型GRPO训练崩溃整理」「如何改善GRPO的稳定性」
- CSDN「强化学习训练避坑指南：为什么我的GRPO模型奖励不收敛」
- 51CTO 同名转载

> ⚠️ **一个必须说在明处的事实**：上面这些二手来源是**同一个内容在多个平台互相转载**。
> 这类来源不能当作独立佐证——**多个博客说同一件事，不等于多个来源证实它**。
> 阶段 4 要把这一条写进收录门槛。

### ⚠️ 第二批对这份预检的更正

**上面这份表有一处结构性遗漏**：它只按「来源的载体」分类（教材 / 文档 / 论文 / 案例），
**没有按「内容形态」分类**。于是**诊断类内容该去哪找，这张表根本没回答**——
而它恰恰是本产品主入口最需要的那一类。

第二批补上了，按**内容形态**重新分渠道：

| 渠道 | 形态 | 能给出什么 | 门槛 |
|---|---|---|---|
| 把失败模式当研究对象的论文 | arXiv / DOI | 现象 + 机理 + **判据** + 修法 | 最硬 |
| 框架官方文档的 FAQ / 配方页 | URL + 锚点 | **唯一自带判据数值** | 要记文档版本 |
| 算法论文正文的失败分析段 | arXiv HTML | 现场描述（**摘要拿不到**） | 必须读全文 |
| 框架 issue tracker | GitHub issue | 真实症状 + 维护者修法 | **只当线索** |

**判别器**：这条来源写不写得出**判据数值**。详见 [`SOURCES.md`](SOURCES.md) §三。

---

## 七、待你拍板的四件事

### ① 「填充数据」= 模型代填，撞你的立项根基

你的红线是「**禁止模型代填内容**」，反向监控指标是「是否出现模型代填」。
但 schema 允许 `filled_by: 模型(vX)`。

我的读法是：**填是允许的，前提是署名 + 不冒充已验证**。所以我的方案是——
`filled_by: 模型(<模型名>)` + `evidence_status: 未验证` + 每条带 `source.ref`，
定位成**「一份有出处的整理」，不是「已验证的知识」**。

**需要你确认：这算不算你禁止的那种「模型代填」？**
- 不算 → 我按上面填
- 算 → 我只立骨架（骨架不是内容），内容留空 + 待填清单，等人填

### ② 来源质量断层怎么处理 —— ✅ 已解决（第二批）

**当时的问题**：以为诊断类知识只有二手来源。三选一是 (a) 收并自曝二手 / (b) 不收 / (c) 改枚举。

**实际的解决方式：都不是。** 三条都没选，因为**问题本身是假的**——
诊断类的一手来源存在，只是**当时的搜索词搜不到它们**（搜「避坑」而非搜「失败模式」）。
更正与四条渠道见 [`SOURCES.md`](SOURCES.md) §三。

**所以 `source.kind` 的枚举一个字都没改**，二手来源的约定（`SOURCES.md` §四）
至今**仍未触发**——第二批 8 条全部来自一手来源。

### ③ 骨架节点的 `filled_by` 写什么（R8 未定案现在到期了）

「立骨架」是第一步，而骨架节点必须写 `filled_by`：
- 写 `未填充` → 每个骨架一条 **R8 WARN**。100 个骨架 = 100 条 WARN，报告会被刷屏
- 写别的（如 `模型(骨架)`）→ R8 不触发，问题绕过去，但 `未填充` 继续是个死值

这正是 R8 判据未定案要回答的问题。**需要你定。**

### ④ 「工程产品」的判据是什么

我理解成：**可校验 + 可 git diff + 零依赖 + 有索引页 + 有来源声明**。
但"产品"可能指别的（要能安装？要有 Web UI？要有 CLI？）。
**需要你给判据**，否则我可能做到一半发现方向不对。
